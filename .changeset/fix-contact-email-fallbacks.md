---
"@firfi/huly-mcp": patch
---

Prevent REST lookup decoding from crashing when Huly returns a null lookup map, and use email SocialIdentity records as the fallback for contact search and email projection.
