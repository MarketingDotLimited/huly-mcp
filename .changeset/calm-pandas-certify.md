---
"@firfi/huly-mcp": patch
"@firfi/huly-cli": patch
---

Complete the direct Effect 4.0.0-rc.108 cutover with certified MCP, HTTP, CLI, schema, lifecycle, package, and local-Huly parity. The supported Node.js floor is now `^22.22.2 || ^24.15.0`; no MCP wire or CLI command contract changes are intended. This remains a patch release because it completes the already-announced release-candidate migration while preserving those public contracts.
