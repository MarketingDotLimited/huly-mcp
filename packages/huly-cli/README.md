# Huly CLI

Command-line frontend for the Huly operations shipped by [`@firfi/huly-mcp`](https://www.npmjs.com/package/@firfi/huly-mcp).

The CLI uses the same schema-owned operation registry as the MCP server. It does not proxy through MCP, JSON-RPC, or `tools/call`.

## Installation

```bash
npx -y @firfi/huly-cli@latest --help
```

Or install it globally:

```bash
npm install -g @firfi/huly-cli
huly --help
```

## Configuration

The CLI reads the same Huly environment variables as the MCP server:

> **Hosted Huly is shutting down.** If you use `https://huly.app`, follow Huly's [migration announcement](https://github.com/hcengineering/platform/blob/develop/README.md), [backup and restore guide](https://github.com/hcengineering/platform/blob/develop/docs/guides/backup-restore.en.md), and [self-hosting instructions](https://github.com/hcengineering/huly-selfhost). Self-hosted deployments are not affected.

```bash
export HULY_URL=https://your-huly-instance.example.com
export HULY_WORKSPACE=yourworkspace
export HULY_EMAIL=your@email.com
export HULY_PASSWORD=yourpassword
```

You can use `HULY_TOKEN` instead of `HULY_EMAIL` and `HULY_PASSWORD`. `HULY_CONNECTION_TIMEOUT` is also supported.

Aggregate CLI analytics are enabled by default and can be disabled with:

```bash
export HULY_CLI_TELEMETRY=0
```

The CLI uses the same PostHog project as `@firfi/huly-mcp`, but events are tagged with `surface=cli` and `package_name=@firfi/huly-cli` so CLI and MCP usage can be separated. Set `HULY_CLI_TELEMETRY_DEBUG=1` to print telemetry debug logs.

## Usage

Every command supports `--json`, `--input-json '<object>'`, and `--input-file path/to/input.json`. Explicit command-line flags override JSON/file input.

```bash
huly projects list
huly projects list --json
huly issues get PROJ 123 --json
huly issues create --project PROJ --title "Fix login flow" --description-file ./body.md
huly comments delete --comment-id 0123456789abcdef --yes
huly attachments download 0123456789abcdef --output ./attachment.bin
huly search "customer import"
```

Run `huly --help` for the generated command tree.

<!-- CLI_COMMAND_REFERENCE_START -->
<!-- Generated from cliCommandCatalog and shared operation schemas. Run `pnpm update-cli-readme`. -->
## Complete command reference

All commands also accept `--json`, `--input-json <object>`, and `--input-file <path>`. Explicit field flags override JSON sources. Structured fields accept JSON. Named positionals are required and are not duplicated as flags.

| Command | Purpose and behavior | Required positionals | Required flags | Optional flags and alternatives |
| --- | --- | --- | --- | --- |
| `huly activity filters list` | List Activity Filters | — | — | `--limit <value>` — Maximum number of activity filters to return (default: 50) |
| `huly activity list` | List Activity | — | — | `--project <value>` — Project identifier for issue activity, e.g. 'HULY'.<br>`--issue-identifier <value>` — Issue identifier for issue activity, e.g. 'HULY-123' or '123'.<br>`--limit <value>` — Maximum number of activity messages to return (default: 50)<br>`--teamspace <value>` — Teamspace name or ID for document activity.<br>`--document <value>` — Document title or ID for document activity.<br>`--channel <value>` — Channel name or ID for channel activity.<br>`--object-id <value>` — Internal Huly object ID to get activity for.<br>`--object-class <value>` — Internal Huly object class for objectId, such as 'tracker:class:Issue'. |
| `huly activity mentions list` | List Mentions | — | — | `--limit <value>` — Maximum number of mentions to return (default: 50) |
| `huly activity messages get <messageId>` | Get Activity Message | `<messageId>` — a string that will be trimmed | — | — |
| `huly activity messages pin <messageId> <pinned>` | Pin Activity Message | `<messageId>` — a string that will be trimmed<br>`<pinned>` — Whether the activity message should be pinned. | — | — |
| `huly activity messages save <messageId>` | Save Message | `<messageId>` — a string that will be trimmed | — | — |
| `huly activity messages unsave <messageId>` | Unsave Message | `<messageId>` — a string that will be trimmed | — | — |
| `huly activity reactions add <messageId> <emoji>` | Add Reaction | `<messageId>` — a string that will be trimmed<br>`<emoji>` — a string that will be trimmed | — | — |
| `huly activity reactions list <messageId>` | List Reactions | `<messageId>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of reactions to return (default: 50) |
| `huly activity reactions remove <messageId> <emoji>` | Remove Reaction Requires `--yes`. | `<messageId>` — a string that will be trimmed<br>`<emoji>` — a string that will be trimmed | — | — |
| `huly activity references list <objectId> <objectClass>` | List Activity References | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed | — | `--direction <value>` — Reference direction. 'from' lists references created by this object, 'to' lists references pointing at this object, 'both' lists either direction (default: both).<br>`--limit <value>` — Maximum number of activity references to return (default: 50) |
| `huly activity replies add <messageId> <body>` | Add Activity Reply | `<messageId>` — a string that will be trimmed<br>`<body>` — Reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--body-file <path>` — read body as text |
| `huly activity replies delete <replyId>` | Delete Activity Reply Requires `--yes`. | `<replyId>` — a string that will be trimmed | — | — |
| `huly activity replies list <messageId>` | List Activity Replies | `<messageId>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of replies to return (default: 50) |
| `huly activity replies update <replyId> <body>` | Update Activity Reply | `<replyId>` — a string that will be trimmed<br>`<body>` — New reply body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--body-file <path>` — read body as text |
| `huly activity saved list` | List Saved Messages | — | — | `--limit <value>` — Maximum number of saved messages to return (default: 50) |
| `huly approvals add <attachedTo> <attachedToClass>` | Add an approval request; pass --requested and --tx as JSON Requires `--yes`. | `<attachedTo>` — a string that will be trimmed<br>`<attachedToClass>` — a string that will be trimmed | `--requested <value>` — People who must decide the approval. Duplicates are collapsed after resolution. Pass arrays or objects as JSON.<br>`--tx <value>` — Opaque Huly SDK transaction applied by Huly when the approval request completes. Pass a real SDK tx payload. | `--space <value>` — a string that will be trimmed<br>`--collection <value>` — a string that will be trimmed<br>`--required-approves-count <value>` — Number of approvals required to complete the request. Defaults to the unique requested person count.<br>`--rejected-tx <value>` — Optional opaque Huly SDK transaction applied by Huly when the approval request is rejected. |
| `huly approvals approve <request>` | Approve a request as the current Huly actor Requires `--yes`. | `<request>` — a string that will be trimmed | — | `--comment <value>` — a string that will be trimmed |
| `huly approvals cancel <request>` | Cancel Approval Request Requires `--yes`. | `<request>` — a string that will be trimmed | — | — |
| `huly approvals comments add <request> <body>` | Add Approval Request Comment | `<request>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly approvals get <request>` | Get Approval Request | `<request>` — a string that will be trimmed | — | — |
| `huly approvals list` | List Approval Requests | — | — | `--status <value>` — Optional approval request status filter.<br>`--attached-to <value>` — a string that will be trimmed<br>`--attached-to-class <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of approval requests to return (default: 50). |
| `huly approvals reject <request> <comment>` | Reject a request as the current Huly actor Requires `--yes`. | `<request>` — a string that will be trimmed<br>`<comment>` — a string that will be trimmed | — | — |
| `huly associations create <sourceClass> <targetClass> <sourceRole> <targetRole> <cardinality>` | Create a generic association definition | `<sourceClass>` — a string that will be trimmed<br>`<targetClass>` — a string that will be trimmed<br>`<sourceRole>` — a string that will be trimmed<br>`<targetRole>` — a string that will be trimmed<br>`<cardinality>` — Association cardinality: one-to-one, one-to-many, many-to-many | — | `--automation-only` / `--no-automation-only` — Whether Huly automation-only UI paths should own relation writes for this association. Defaults to false.<br>`--if-exists <value>` — return_existing (default) returns an identical existing association; fail reports an existing association as an error |
| `huly associations delete <association>` | Delete an unused association definition Requires `--yes`. | `<association>` — a string that will be trimmed | — | — |
| `huly attachments add <objectId> <objectClass> <space> <filename> <contentType>` | Add an attachment to a raw Huly object. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed<br>`<space>` — a string that will be trimmed<br>`<filename>` — a string that will be trimmed<br>`<contentType>` — a string that will be trimmed | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false)<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment).<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments add-to-document` | Add an attachment to a document. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed<br>`--filename <value>` — a string that will be trimmed<br>`--content-type <value>` — a string that will be trimmed | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false)<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment).<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments add-to-issue` | Add an attachment to an issue. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--project <value>` — a string that will be trimmed<br>`--identifier <value>` — a string that will be trimmed<br>`--filename <value>` — a string that will be trimmed<br>`--content-type <value>` — a string that will be trimmed | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Attachment description<br>`--pinned` / `--no-pinned` — Whether to pin the attachment (default: false)<br>`--kind <value>` — Attachment subclass to create: attachment, embedding, or photo (default: attachment).<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly attachments delete <attachmentId>` | Delete Attachment Requires `--yes`. | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments download <attachmentId>` | Resolve or download an attachment Supports `--output <path>`. | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments get <attachmentId>` | Get attachment metadata | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments list` | List attachments | — | `--object-id <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed | `--limit <value>` — Maximum number of attachments to return (default: 50) |
| `huly attachments pin <attachmentId> <pinned>` | Pin Attachment | `<attachmentId>` — a string that will be trimmed<br>`<pinned>` — Whether to pin (true) or unpin (false) | — | — |
| `huly attachments read-image <attachmentId>` | Read a supported image attachment; use --output to write its decoded bytes Supports `--output <path>`. | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments save <attachmentId>` | Save Attachment | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments saved list` | List Saved Attachments | — | — | `--limit <value>` — Maximum number of saved attachments to return (default: 50) |
| `huly attachments unsave <attachmentId>` | Unsave Attachment | `<attachmentId>` — a string that will be trimmed | — | — |
| `huly attachments update <attachmentId>` | Update Attachment | `<attachmentId>` — a string that will be trimmed | — | `--description <value>` — New description (null to clear) Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Pin or unpin the attachment<br>`--description-file <path>` — read description as text |
| `huly boards archive <board>` | Archive Board | `<board>` — a string that will be trimmed | — | — |
| `huly boards cards archive <board> <card>` | Archive Board Card | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly boards cards create <board> <title>` | Create Board Card | `<board>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed | — | `--description <value>` — Board card description in markdown. Stored as inline Huly Markup. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--kind <value>` — a string that will be trimmed<br>`--status <value>` — a string that will be trimmed<br>`--assignee <value>` — Assignee Employee _id, exact email address, or exact person display name.<br>`--members <value>` — Initial card members. Each entry accepts Employee _id, exact email, or exact person display name. Pass arrays or objects as JSON.<br>`--location <value>` — Optional card location text.<br>`--cover <value>` — Cover settings for a board card. Pass arrays or objects as JSON.<br>`--start-date <value>` — Start date timestamp in milliseconds.<br>`--due-date <value>` — Due date timestamp in milliseconds.<br>`--description-file <path>` — read description as text |
| `huly boards cards delete <board> <card>` | Delete Board Card Requires `--yes`. | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly boards cards get <board> <card>` | Get Board Card | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly boards cards labels add <board> <card> <label>` | Add Board Card Label | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | — | `--color <value>` — Color for the board label definition when label is a new title (default: 0). Existing labels are not recolored.<br>`--category <value>` — a string that will be trimmed |
| `huly boards cards labels list <board> <card>` | List Board Card Labels | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly boards cards labels remove <board> <card> <label>` | Remove Board Card Label Requires `--yes`. | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | — | — |
| `huly boards cards list <board>` | List Board Cards | `<board>` — a string that will be trimmed | — | `--include-archived` / `--no-include-archived` — Include archived board cards in results (default: false, showing only active cards).<br>`--title-search <value>` — Search board cards by title substring (case-insensitive SQL LIKE).<br>`--limit <value>` — Maximum number of board cards to return (default: 50). |
| `huly boards cards unarchive <board> <card>` | Unarchive Board Card Requires `--yes`. | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly boards cards update <board> <card>` | Update Board Card | `<board>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — New board card description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--status <value>` — a string that will be trimmed<br>`--assignee <value>` — New assignee Employee _id, exact email, or exact person display name; null unassigns. Pass null to clear the field.<br>`--members <value>` — Replace card members with this exact list. Cannot be combined with addMembers or removeMembers. Pass arrays or objects as JSON.<br>`--add-members <value>` — Members to add without replacing existing members. Pass arrays or objects as JSON.<br>`--remove-members <value>` — Members to remove without replacing existing members. Pass arrays or objects as JSON.<br>`--location <value>` — New card location. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--cover <value>` — New card cover; null clears the cover. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--start-date <value>` — New start date timestamp in milliseconds; null clears it. Pass null to clear the field.<br>`--due-date <value>` — New due date timestamp in milliseconds; null clears it. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly boards common-preference get` | Get Board Common Preference | — | — | — |
| `huly boards create <name>` | Create Board | `<name>` — a string that will be trimmed | — | `--description <value>` — Plain text board description.<br>`--private` / `--no-private` — Whether the board is private (default: false).<br>`--project-type <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly boards get <board>` | Get Board | `<board>` — a string that will be trimmed | — | — |
| `huly boards labels create <title>` | Create Board Label | `<title>` — a string that will be trimmed | — | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0).<br>`--description <value>` — Board label description.<br>`--category <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly boards labels delete <label>` | Delete Board Label Requires `--yes`. | `<label>` — a string that will be trimmed | — | — |
| `huly boards labels list` | List Board Labels | — | — | `--category <value>` — a string that will be trimmed<br>`--title-search <value>` — Optional board label title substring search.<br>`--limit <value>` — Maximum number of board labels to return (default: 50). |
| `huly boards labels update <label>` | Update Board Label | `<label>` — Board label TagElement _id or exact title. | — | `--title <value>` — New board label title.<br>`--color <value>` — New Huly platform color palette index from 0 through 23.<br>`--description <value>` — New board label description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--category <value>` — New board label category _id or exact label.<br>`--description-file <path>` — read description as text |
| `huly boards list` | List Boards | — | — | `--include-archived` / `--no-include-archived` — Include archived boards in results (default: false, showing only active boards).<br>`--limit <value>` — Maximum number of boards to return (default: 50). |
| `huly boards menu-pages list` | List Board Menu Pages | — | — | `--page <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of board menu pages to return (default: 50). |
| `huly boards saved-views get <savedView>` | Get Board Saved View | `<savedView>` — FilteredView _id or exact saved view name scoped to attachedTo = board.app.Board. | — | — |
| `huly boards saved-views list` | List Board Saved Views | — | — | `--visibility <value>` — Filter board saved views by whether the current account is in the saved view users list.<br>`--name-search <value>` — Optional saved view name substring search.<br>`--limit <value>` — Maximum number of board saved views to return (default: 50). |
| `huly boards unarchive <board>` | Unarchive Board Requires `--yes`. | `<board>` — a string that will be trimmed | — | — |
| `huly boards update <board>` | Update Board | `<board>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New plain text board description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the board is private.<br>`--description-file <path>` — read description as text |
| `huly boards viewlets list` | List Board Viewlets | — | — | `--viewlet <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of board viewlets to return (default: 50). |
| `huly calendar calendars list` | List Calendars | — | — | — |
| `huly calendar events create <title> <date>` | Create a calendar event | `<title>` — a string that will be trimmed<br>`<date>` — Start date/time (timestamp) | — | `--description <value>` — Event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--due-date <value>` — End date/time (timestamp). If omitted, Huly MCP uses date + 1 hour.<br>`--all-day` / `--no-all-day` — All-day event (default: false)<br>`--location <value>` — Event location<br>`--participants <value>` — Participants to invite. Each entry may be a plain email string or an object with email, exact name, or personId. Pass arrays or objects as JSON.<br>`--external-participants <value>` — External participant email addresses that are not resolvable workspace contacts. Pass arrays or objects as JSON.<br>`--reminders <value>` — Reminder timestamps in milliseconds. Pass arrays or objects as JSON.<br>`--access <value>` — Event access level.<br>`--time-zone <value>` — a string that will be trimmed<br>`--block-time` / `--no-block-time` — Whether this event blocks the user's time on the calendar.<br>`--visibility <value>` — Event visibility (public, freeBusy, private)<br>`--calendar-id <value>` — a string that will be trimmed<br>`--calendar-name <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly calendar events delete <eventId>` | Delete Event Requires `--yes`. | `<eventId>` — a string that will be trimmed | — | — |
| `huly calendar events get <eventId>` | Get Event | `<eventId>` — a string that will be trimmed | — | — |
| `huly calendar events instances list <recurringEventId>` | List Event Instances | `<recurringEventId>` — a string that will be trimmed | — | `--from <value>` — Start date filter (timestamp)<br>`--to <value>` — End date filter (timestamp)<br>`--limit <value>` — Maximum number of instances to return (default: 50)<br>`--include-participants` / `--no-include-participants` — Include full participant info (requires extra lookups, default: false) |
| `huly calendar events list` | List Events | — | — | `--from <value>` — Start date filter (timestamp)<br>`--to <value>` — End date filter (timestamp)<br>`--limit <value>` — Maximum number of events to return (default: 50) |
| `huly calendar events recurring create <title> <startDate>` | Create a recurring event; pass --rules as JSON | `<title>` — a string that will be trimmed<br>`<startDate>` — First occurrence start date/time (timestamp) | `--rules <value>` — Recurring rules (RFC5545 RRULE format) Pass arrays or objects as JSON. | `--description <value>` — Event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--due-date <value>` — First occurrence end date/time (timestamp). If omitted, Huly MCP uses startDate + 1 hour.<br>`--all-day` / `--no-all-day` — All-day event (default: false)<br>`--location <value>` — Event location<br>`--participants <value>` — Participants to invite. Each entry may be a plain email string or an object with email, exact name, or personId. Pass arrays or objects as JSON.<br>`--external-participants <value>` — External participant email addresses that are not resolvable workspace contacts. Pass arrays or objects as JSON.<br>`--reminders <value>` — Reminder timestamps in milliseconds. Pass arrays or objects as JSON.<br>`--time-zone <value>` — a string that will be trimmed<br>`--access <value>` — Event access level.<br>`--block-time` / `--no-block-time` — Whether this event blocks the user's time on the calendar.<br>`--visibility <value>` — Event visibility (public, freeBusy, private)<br>`--calendar-id <value>` — a string that will be trimmed<br>`--calendar-name <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly calendar events recurring list` | List Recurring Events | — | — | `--limit <value>` — Maximum number of recurring events to return (default: 50) |
| `huly calendar events update <eventId>` | Update a calendar event | `<eventId>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — New event description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--date <value>` — New start date/time (timestamp)<br>`--due-date <value>` — New end date/time (timestamp)<br>`--all-day` / `--no-all-day` — All-day event<br>`--location <value>` — New event location. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--visibility <value>` — New event visibility<br>`--participants <value>` — Replace all workspace participants with these resolved participants. Pass arrays or objects as JSON.<br>`--add-participants <value>` — Resolve and add these workspace participants, preserving existing participants. Pass arrays or objects as JSON.<br>`--remove-participants <value>` — Resolve and remove these workspace participants, preserving other participants. Pass arrays or objects as JSON.<br>`--external-participants <value>` — Replace all external participant email addresses. Pass arrays or objects as JSON.<br>`--add-external-participants <value>` — Add external participant email addresses, preserving existing external participants. Pass arrays or objects as JSON.<br>`--remove-external-participants <value>` — Remove external participant email addresses, preserving other external participants. Pass arrays or objects as JSON.<br>`--reminders <value>` — Replace event reminders with these reminder timestamps. Pass arrays or objects as JSON.<br>`--access <value>` — New event access level.<br>`--time-zone <value>` — a string that will be trimmed<br>`--block-time` / `--no-block-time` — Whether this event blocks time.<br>`--calendar-id <value>` — a string that will be trimmed<br>`--calendar-name <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly calendar schedules create <title>` | Create a calendar schedule; pass --availability as JSON | `<title>` — a string that will be trimmed | `--meeting-duration <value>` — Default meeting duration in minutes.<br>`--meeting-interval <value>` — Minimum interval between meetings in minutes.<br>`--availability <value>` — Pass arrays or objects as JSON.<br>`--time-zone <value>` — a string that will be trimmed | `--owner <value>` — a string that will be trimmed<br>`--description <value>` — Schedule description.<br>`--calendar-id <value>` — a string that will be trimmed<br>`--calendar-name <value>` — a string that will be trimmed |
| `huly calendar schedules delete <scheduleId>` | Delete Schedule Requires `--yes`. | `<scheduleId>` — a string that will be trimmed | — | — |
| `huly calendar schedules get <scheduleId>` | Get Schedule | `<scheduleId>` — a string that will be trimmed | — | — |
| `huly calendar schedules list` | List Schedules | — | — | `--owner <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of schedules to return (default: 50). |
| `huly calendar schedules update <scheduleId>` | Update a calendar schedule; pass --availability as JSON | `<scheduleId>` — a string that will be trimmed | — | `--owner <value>` — a string that will be trimmed<br>`--title <value>` — a string that will be trimmed<br>`--description <value>` — New schedule description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--meeting-duration <value>` — New default meeting duration in minutes.<br>`--meeting-interval <value>` — New minimum interval between meetings in minutes.<br>`--availability <value>` — Pass arrays or objects as JSON.<br>`--time-zone <value>` — a string that will be trimmed<br>`--calendar-id <value>` — a string that will be trimmed<br>`--calendar-name <value>` — a string that will be trimmed |
| `huly cards comments add <cardSpace> <card> <body>` | Add Card Comment | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly cards comments delete <cardSpace> <card> <commentId>` | Delete Card Comment Requires `--yes`. | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed<br>`<commentId>` — a string that will be trimmed | — | — |
| `huly cards comments list <cardSpace> <card>` | List Card Comments | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of comments to return (default: 50) |
| `huly cards comments update <cardSpace> <card> <commentId> <body>` | Update Card Comment | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed<br>`<commentId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly cards create <cardSpace> <type> <title>` | Create Card | `<cardSpace>` — a string that will be trimmed<br>`<type>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed | — | `--content <value>` — Card content in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--parent <value>` — a string that will be trimmed<br>`--content-file <path>` — read content as text |
| `huly cards delete <cardSpace> <card>` | Delete Card Requires `--yes`. | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly cards get <cardSpace> <card>` | Get Card | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly cards list <cardSpace>` | List Cards | `<cardSpace>` — a string that will be trimmed | — | `--type <value>` — a string that will be trimmed<br>`--title-search <value>` — Search cards by title substring (case-insensitive). Mutually exclusive with titleRegex.<br>`--title-regex <value>` — Filter cards by title using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole title: use '%' for any string (e.g., '%TODO%' contains, 'TODO%' prefix). Mutually exclusive with titleSearch; use titleSearch for simple substring matching.<br>`--content-search <value>` — Search cards by content (fulltext search)<br>`--limit <value>` — Maximum number of cards to return (default: 50) |
| `huly cards master-tags list <cardSpace>` | List Master Tags | `<cardSpace>` — a string that will be trimmed | — | — |
| `huly cards spaces list` | List Card Spaces | — | — | `--include-archived` / `--no-include-archived` — Include archived card spaces in results (default: false, showing only active)<br>`--limit <value>` — Maximum number of card spaces to return (default: 50) |
| `huly cards update <cardSpace> <card>` | Update Card | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--content <value>` — New card content in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--content-file <path>` — read content as text |
| `huly cards versions list <cardSpace> <card>` | List Card Versions | `<cardSpace>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | `--limit <value>` — Maximum versions in this page (default: 50). total always counts the full history. |
| `huly channels access request <channel>` | Request Channel Access Requires `--yes`. | `<channel>` — a string that will be trimmed | — | — |
| `huly channels archive <channel>` | Archive Channel | `<channel>` — a string that will be trimmed | — | — |
| `huly channels conversations closed set <closed>` | Set Conversation Closed | `<closed>` — True to close/hide this conversation for the authenticated user, false to reopen it. Does not leave channels or remove members. | — | `--channel <value>`<br>`--dm <value>` — a string that will be trimmed |
| `huly channels conversations starred set <starred>` | Set Conversation Starred | `<starred>` — True to star/pin this conversation for the authenticated user, false to unstar it. | — | `--channel <value>`<br>`--dm <value>` — a string that will be trimmed |
| `huly channels create <name>` | Create Channel | `<name>` — a string that will be trimmed | — | `--topic <value>` — Channel topic/description<br>`--private` / `--no-private` — Whether channel is private (default: false) |
| `huly channels delete <channel>` | Delete Channel Requires `--yes`. | `<channel>` — a string that will be trimmed | — | — |
| `huly channels direct-messages create <person>` | Create Direct Message | `<person>` — Participant to open a one-to-one DM with: email address or exact display name (e.g. `Smith,Bill`). Resolved via the Employee mixin to a Huly account. | — | — |
| `huly channels direct-messages list` | List Direct Messages | — | — | `--limit <value>` — Maximum number of DM conversations to return (default: 50) |
| `huly channels direct-messages messages delete <dm> <messageId>` | Delete a direct-message message Requires `--yes`. | `<dm>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed | — | — |
| `huly channels direct-messages messages list <dm>` | List Dm Messages | `<dm>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of messages to return (default: 50) |
| `huly channels direct-messages messages send <dm> <body>` | Send a direct message | `<dm>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels direct-messages messages update <dm> <messageId> <body>` | Update a direct-message message | `<dm>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels external-messages list <provider> <channel>` | List External Channel Messages | `<provider>` — External provider to read or assess. Telegram reads persisted messages when its published model and an exact channel are available; Gmail remains an explicit compatibility assessment.<br>`<channel>` — a string that will be trimmed | — | `--limit <value>` — Requested maximum message count to echo in the result (default: 50, max: 200). |
| `huly channels get <channel>` | Get Channel | `<channel>` — a string that will be trimmed | — | — |
| `huly channels group-direct-messages create <people>` | Create Group Direct Message | `<people>` — At least two other workspace members to include in a group DM. Each entry accepts an exact email address or exact person display name. The authenticated account is included automatically. Pass arrays or objects as JSON. | — | — |
| `huly channels join <channel>` | Join Channel Requires `--yes`. | `<channel>` — a string that will be trimmed | — | — |
| `huly channels leave <channel>` | Leave Channel Requires `--yes`. | `<channel>` — a string that will be trimmed | — | — |
| `huly channels list` | List Channels | — | — | `--name-search <value>` — Search channels by name substring (case-insensitive). Mutually exclusive with nameRegex.<br>`--name-regex <value>` — Filter channels by name using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole name: use '%' for any string (e.g., '%dev%' contains, 'dev%' prefix). Mutually exclusive with nameSearch; use nameSearch for simple substring matching.<br>`--topic-search <value>` — Search channels by topic substring (case-insensitive)<br>`--limit <value>` — Maximum number of channels to return (default: 50)<br>`--include-archived` / `--no-include-archived` — Include archived channels in results (default: false) |
| `huly channels members add <channel> <members>` | Add Channel Members | `<channel>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person display name. Pass arrays or objects as JSON. | — | — |
| `huly channels members list <channel>` | List Channel Members | `<channel>` — a string that will be trimmed | — | — |
| `huly channels members remove <channel> <members>` | Remove Channel Members Requires `--yes`. | `<channel>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person display name. Pass arrays or objects as JSON. | — | — |
| `huly channels messages attachments add` | Attach a file to a channel, direct-message, or thread message; pass --target as JSON. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | — | `--target <value>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Pass arrays or objects as JSON.<br>`--filename <value>` — Name of the file to attach to the chat message or thread reply.<br>`--content-type <value>` — MIME type of the file, such as image/png or application/pdf. | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional attachment description. Use null on update to clear it.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned.<br>`--description-file <path>` — read description as text<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly channels messages attachments delete <target> <attachmentId>` | Delete Chat Message Attachment Requires `--yes`. | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. | — | — |
| `huly channels messages attachments get <target> <attachmentId>` | Get Chat Message Attachment | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. | — | — |
| `huly channels messages attachments list <target>` | List Chat Message Attachments | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching attachments to return (default: 50). |
| `huly channels messages attachments update <target> <attachmentId>` | Update Chat Message Attachment | `<target>` — Chat attachment target. Use channel_message for a channel message, dm_message for a direct-message message, or thread_reply for a thread reply. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved chat message target. | — | `--description <value>` — Optional attachment description. Use null on update to clear it. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned.<br>`--description-file <path>` — read description as text |
| `huly channels messages delete <channel> <messageId>` | Delete a channel message Requires `--yes`. | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed | — | — |
| `huly channels messages list <channel>` | List Channel Messages | `<channel>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of messages to return (default: 50) |
| `huly channels messages pinned list` | List Pinned Chat Messages | — | — | `--channel <value>`<br>`--dm <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum pinned messages to return (default: 50). |
| `huly channels messages pinned set <messageId> <pinned>` | Set Chat Message Pinned | `<messageId>` — a string that will be trimmed<br>`<pinned>` — True to pin the message, false to unpin it. | — | `--channel <value>`<br>`--dm <value>` — a string that will be trimmed |
| `huly channels messages send <channel> <body>` | Send a message to a channel | `<channel>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels messages translate <messageId> <targetLanguage>` | Translate Chat Message Requires `--yes`. | `<messageId>` — a string that will be trimmed<br>`<targetLanguage>` — a string that will be trimmed | — | `--channel <value>`<br>`--dm <value>` — a string that will be trimmed |
| `huly channels messages update <channel> <messageId> <body>` | Update a channel message | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels threads replies add <channel> <messageId> <body>` | Reply to a channel message | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels threads replies delete <channel> <messageId> <replyId>` | Delete a thread reply Requires `--yes`. | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed<br>`<replyId>` — a string that will be trimmed | — | — |
| `huly channels threads replies list <channel> <messageId>` | List Thread Replies | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of replies to return (default: 50) |
| `huly channels threads replies update <channel> <messageId> <replyId> <body>` | Update a thread reply | `<channel>` — a string that will be trimmed<br>`<messageId>` — a string that will be trimmed<br>`<replyId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--body-file <path>` — read body as text |
| `huly channels unarchive <channel>` | Unarchive Channel Requires `--yes`. | `<channel>` — a string that will be trimmed | — | — |
| `huly channels update <channel>` | Update Channel | `<channel>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--topic <value>` — New channel topic. Pass null to clear; empty string is also accepted. Pass null to clear the field. |
| `huly collaborators object list` | List Object Collaborators | — | — | `--object-id <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed<br>`--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of collaborators to return (default: 50) |
| `huly comments add` | Add an issue comment | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--body <value>` — a string that will be trimmed | `--body-file <path>` — read body as text |
| `huly comments delete` | Delete an issue comment Requires `--yes`. | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--comment-id <value>` — a string that will be trimmed | — |
| `huly comments list` | List issue comments | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed | `--limit <value>` — Maximum number of comments to return (default: 50) |
| `huly comments update` | Update an issue comment | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--comment-id <value>` — a string that will be trimmed<br>`--body <value>` — a string that will be trimmed | `--body-file <path>` — read body as text |
| `huly components create <project> <label>` | Create Component | `<project>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | — | `--description <value>` — Component description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--lead <value>` — Lead person email address or display name<br>`--description-file <path>` — read description as text |
| `huly components delete <project> <component>` | Delete Component Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<component>` — a string that will be trimmed | — | — |
| `huly components get` | Get a component | — | `--project <value>` — a string that will be trimmed<br>`--component <value>` — a string that will be trimmed | — |
| `huly components list` | List components | — | `--project <value>` — a string that will be trimmed | `--limit <value>` — Maximum number of components to return (default: 50) |
| `huly components update <project> <component>` | Update Component | `<project>` — a string that will be trimmed<br>`<component>` — a string that will be trimmed | — | `--label <value>` — a string that will be trimmed<br>`--description <value>` — New component description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--lead <value>` — New lead person email or display name (null to unassign) Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly contacts channel-providers list` | List Contact Channel Providers | — | — | — |
| `huly contacts employees list` | List Employees | — | — | `--limit <value>` — Maximum number of employees to return (default: 50) |
| `huly contacts organizations channels add <provider> <value> <organizationId>` | Add Organization Channel | `<provider>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`<value>` — a string that will be trimmed<br>`<organizationId>` — a string that will be trimmed | — | — |
| `huly contacts organizations channels list <organizationId>` | List Organization Channels | `<organizationId>` — a string that will be trimmed | — | — |
| `huly contacts organizations channels remove <organizationId>` | Remove Organization Channel Requires `--yes`. | `<organizationId>` — a string that will be trimmed | — | `--channel-id <value>` — a string that will be trimmed<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--value <value>` — a string that will be trimmed |
| `huly contacts organizations channels update <organizationId>` | Update Organization Channel | `<organizationId>` — a string that will be trimmed | — | `--channel-id <value>` — a string that will be trimmed<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--value <value>` — a string that will be trimmed<br>`--new-provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--new-value <value>` — a string that will be trimmed |
| `huly contacts organizations create <name>` | Create Organization | `<name>` — a string that will be trimmed | — | `--members <value>` — Member person IDs or emails Pass arrays or objects as JSON. |
| `huly contacts organizations customer make <identifier>` | Make Organization Customer Requires `--yes`. | `<identifier>` — a string that will be trimmed | — | — |
| `huly contacts organizations delete <identifier>` | Delete Organization Requires `--yes`. | `<identifier>` — a string that will be trimmed | — | — |
| `huly contacts organizations get <identifier>` | Get Organization | `<identifier>` — a string that will be trimmed | — | — |
| `huly contacts organizations list` | List Organizations | — | — | `--limit <value>` — Maximum number of organizations to return (default: 50) |
| `huly contacts organizations members add <organizationId> <personIdentifier>` | Add Organization Member | `<organizationId>` — a string that will be trimmed<br>`<personIdentifier>` — a string that will be trimmed | — | — |
| `huly contacts organizations members list <organizationId>` | List Organization Members | `<organizationId>` — a string that will be trimmed | — | — |
| `huly contacts organizations members remove <organizationId> <personIdentifier>` | Remove Organization Member Requires `--yes`. | `<organizationId>` — a string that will be trimmed<br>`<personIdentifier>` — a string that will be trimmed | — | — |
| `huly contacts organizations update <identifier>` | Update Organization | `<identifier>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--city <value>` — New city (null to clear) Pass null to clear the field.<br>`--description <value>` — New description/notes in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly contacts persons channels add <provider> <value> <person>` | Add Person Channel | `<provider>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`<value>` — a string that will be trimmed<br>`<person>` — a string that will be trimmed | — | — |
| `huly contacts persons channels list <person>` | List Person Channels | `<person>` — a string that will be trimmed | — | — |
| `huly contacts persons channels remove <person>` | Remove Person Channel Requires `--yes`. | `<person>` — a string that will be trimmed | — | `--channel-id <value>` — a string that will be trimmed<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--value <value>` — a string that will be trimmed |
| `huly contacts persons channels update <person>` | Update Person Channel | `<person>` — a string that will be trimmed | — | `--channel-id <value>` — a string that will be trimmed<br>`--provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--value <value>` — a string that will be trimmed<br>`--new-provider <value>` — Channel provider label: email, phone, linkedin, twitter, github, facebook, telegram, homepage, whatsapp, skype, profile, viber.<br>`--new-value <value>` — a string that will be trimmed |
| `huly contacts persons create <firstName> <lastName>` | Create Person | `<firstName>` — a string that will be trimmed<br>`<lastName>` — a string that will be trimmed | — | `--email <value>` — Email address<br>`--city <value>` — City |
| `huly contacts persons delete <personId>` | Delete Person Requires `--yes`. | `<personId>` — a string that will be trimmed | — | — |
| `huly contacts persons get` | Get Person | — | — | `--person-id <value>` — a string that will be trimmed<br>`--email <value>` — Person email address |
| `huly contacts persons list` | List Persons | — | — | `--name-search <value>` — Search persons by name substring (case-insensitive). Mutually exclusive with nameRegex.<br>`--name-regex <value>` — Filter persons by name using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole name: use '%' for any string (e.g., '%Smith%' contains, 'Smith%' prefix). Mutually exclusive with nameSearch; use nameSearch for simple substring matching.<br>`--email-search <value>` — Search persons by email substring (case-insensitive)<br>`--limit <value>` — Maximum number of persons to return (default: 50) |
| `huly contacts persons organizations list` | List Person Organizations | — | — | `--person-id <value>` — a string that will be trimmed<br>`--email <value>` — Person email address |
| `huly contacts persons update <personId>` | Update Person | `<personId>` — a string that will be trimmed | — | `--first-name <value>` — a string that will be trimmed<br>`--last-name <value>` — a string that will be trimmed<br>`--city <value>` — New city (null to clear) Pass null to clear the field. |
| `huly custom-fields list` | List Custom Fields | — | — | `--target-class <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of fields to return (default: 200) |
| `huly custom-fields values get <objectId> <objectClass>` | Get Custom Field Values | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed | — | — |
| `huly deletion preview <entityType> <project>` | Preview Deletion | `<entityType>` — Type of entity: issue, project, component, milestone<br>`<project>` — a string that will be trimmed | — | `--identifier <value>` — Entity identifier within the project. Required for issue (e.g., 'PROJ-123' or number), component (label or ID), milestone (label or ID). Ignored for entityType='project'. |
| `huly documents comments` | List document inline comments | — | `--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed | `--include-replies` / `--no-include-replies` — Include thread reply messages for each inline comment (default: false) |
| `huly documents create` | Create a document | — | `--teamspace <value>` — a string that will be trimmed<br>`--title <value>` — a string that will be trimmed | `--content <value>` — Document content in markdown. Use markdown links to current-workspace Huly browse URLs for native references; Huly browse links returned in get_document content round-trip as native references. The URL identifies the object; link text is display text; plain issue keys stay text. External URLs stay normal markdown links.<br>`--parent <value>` — a string that will be trimmed<br>`--content-file <path>` — read content as text |
| `huly documents delete <teamspace> <document>` | Delete Document Requires `--yes`. | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed | — | — |
| `huly documents edit` | Edit a document | — | `--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed | `--title <value>` — a string that will be trimmed<br>`--content <value>` — Full replacement content in markdown. Mutually exclusive with old_text/new_text. Use markdown links to current-workspace Huly browse URLs for native references; Huly browse links returned in get_document content round-trip as native references. The URL identifies the object; link text is display text; plain issue keys stay text. External URLs stay normal markdown links.<br>`--old-text <value>` — Exact text to find in the document. Must be non-empty. Mutually exclusive with content.<br>`--new-text <value>` — Replacement text. Empty string deletes the matched text. Required when old_text is provided.<br>`--replace-all` / `--no-replace-all` — Replace all occurrences of old_text (default: false). Only used with old_text/new_text.<br>`--content-file <path>` — read content as text<br>`--old-text-file <path>` — read old_text as text<br>`--new-text-file <path>` — read new_text as text |
| `huly documents get` | Get a document | — | `--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed | — |
| `huly documents labels add <teamspace> <document> <label>` | Add Document Label | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | — | `--color <value>` — Color for a newly created label definition from 0 through 23 (default: 0). Ignored for an existing label. |
| `huly documents labels definitions list` | List Document Label Definitions | — | — | `--title-search <value>` — Optional label title substring search.<br>`--limit <value>` — Maximum number of label definitions to return (default: 50). |
| `huly documents labels list <teamspace> <document>` | List Document Labels | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed | — | — |
| `huly documents labels remove <teamspace> <document> <label>` | Remove Document Label Requires `--yes`. | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | — | — |
| `huly documents list` | List documents | — | `--teamspace <value>` — a string that will be trimmed | `--title-search <value>` — Search documents by title substring (case-insensitive). Mutually exclusive with titleRegex.<br>`--title-regex <value>` — Filter documents by title using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole title: use '%' for any string (e.g., '%RFC%' contains, 'RFC%' prefix). Mutually exclusive with titleSearch; use titleSearch for simple substring matching.<br>`--content-search <value>` — Search documents by content (fulltext search)<br>`--limit <value>` — Maximum number of documents to return (default: 50) |
| `huly documents snapshots get <teamspace> <document> <snapshot>` | Get Document Snapshot | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed<br>`<snapshot>` — a string that will be trimmed | — | — |
| `huly documents snapshots list <teamspace> <document>` | List Document Snapshots | `<teamspace>` — a string that will be trimmed<br>`<document>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of snapshots to return (default: 50). |
| `huly drawings create <parentId> <parentClass> <space>` | Create a drawing; pass opaque content with --content or --content-file | `<parentId>` — a string that will be trimmed<br>`<parentClass>` — a string that will be trimmed<br>`<space>` — a string that will be trimmed | — | `--content <value>` — Opaque drawing content payload stored by Huly.<br>`--content-file <path>` — read content as text |
| `huly drawings delete <drawingId>` | Delete Drawing Requires `--yes`. | `<drawingId>` — a string that will be trimmed | — | — |
| `huly drawings get <drawingId>` | Get Drawing | `<drawingId>` — a string that will be trimmed | — | — |
| `huly drawings list <parentId> <parentClass>` | List Drawings | `<parentId>` — a string that will be trimmed<br>`<parentClass>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of drawings to return (default: 50) |
| `huly drawings update <drawingId>` | Update drawing content; pass null to --content to clear it | `<drawingId>` — a string that will be trimmed | `--content <value>` — New drawing content payload. Use null to clear. Pass null to clear the field. | `--content-file <path>` — read content as text |
| `huly drive create <name>` | Create Drive | `<name>` — a string that will be trimmed | — | `--description <value>` — Plain-text Drive description.<br>`--private` / `--no-private` — Whether the Drive is private. Defaults to false.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the Drive. Defaults to false.<br>`--members <value>` — Initial Drive members. Each entry may be an account UUID, exact email address, or exact person name. When omitted, the caller is added. Pass arrays or objects as JSON.<br>`--owners <value>` — Initial Drive owners. Each entry may be an account UUID, exact email address, or exact person name. When omitted, the caller is the owner. Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly drive delete <drive>` | Delete Drive Requires `--yes`. | `<drive>` — a string that will be trimmed | — | — |
| `huly drive files activity list <drive>` | List Drive File Activity | `<drive>` — a string that will be trimmed | — | `--file-path <value>` — a string that will be trimmed<br>`--file-id <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of activity messages to return (default: 50). |
| `huly drive files comments add <drive> <body>` | Add Drive File Comment | `<drive>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--file-path <value>` — a string that will be trimmed<br>`--file-id <value>` — a string that will be trimmed<br>`--body-file <path>` — read body as text |
| `huly drive files comments delete <drive> <commentId>` | Delete Drive File Comment Requires `--yes`. | `<drive>` — a string that will be trimmed<br>`<commentId>` — a string that will be trimmed | — | `--file-path <value>` — a string that will be trimmed<br>`--file-id <value>` — a string that will be trimmed |
| `huly drive files comments list <drive>` | List Drive File Comments | `<drive>` — a string that will be trimmed | — | `--file-path <value>` — a string that will be trimmed<br>`--file-id <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of comments to return (default: 50). |
| `huly drive files comments update <drive> <commentId> <body>` | Update Drive File Comment | `<drive>` — a string that will be trimmed<br>`<commentId>` — a string that will be trimmed<br>`<body>` — a string that will be trimmed | — | `--file-path <value>` — a string that will be trimmed<br>`--file-id <value>` — a string that will be trimmed<br>`--body-file <path>` — read body as text |
| `huly drive files upload <drive> <path> <contentType>` | Upload a drive file. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<drive>` — a string that will be trimmed<br>`<path>` — a string that will be trimmed<br>`<contentType>` — a string that will be trimmed | — | `--create-parents` / `--no-create-parents` — Create missing parent folders automatically. Defaults to true.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly drive files versions list <drive> <file>` | List Drive File Versions | `<drive>` — a string that will be trimmed<br>`<file>` — a string that will be trimmed | — | — |
| `huly drive files versions restore <drive> <file> <version>` | Restore Drive File Version | `<drive>` — a string that will be trimmed<br>`<file>` — a string that will be trimmed<br>`<version>` — a string that will be trimmed | — | — |
| `huly drive files versions upload <drive> <file> <contentType>` | Upload a new drive-file version. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<drive>` — a string that will be trimmed<br>`<file>` — a string that will be trimmed<br>`<contentType>` — a string that will be trimmed | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly drive folders create <drive> <path>` | Create Drive Folder | `<drive>` — a string that will be trimmed<br>`<path>` — a string that will be trimmed | — | — |
| `huly drive get <drive>` | Get Drive | `<drive>` — a string that will be trimmed | — | — |
| `huly drive items delete <drive>` | Delete Drive Item Requires `--yes`. | `<drive>` — a string that will be trimmed | — | `--path <value>` — a string that will be trimmed<br>`--item-id <value>` — a string that will be trimmed |
| `huly drive items get <drive>` | Get Drive Item | `<drive>` — a string that will be trimmed | — | `--path <value>` — a string that will be trimmed<br>`--item-id <value>` — a string that will be trimmed |
| `huly drive items list <drive>` | List Drive Items | `<drive>` — a string that will be trimmed | — | `--path <value>` — a string that will be trimmed<br>`--kind <value>` — Filter returned children by kind. Defaults to any.<br>`--limit <value>` — Maximum drive items to return (default: 50). |
| `huly drive items move <drive> <targetFolderPath>` | Move Drive Item | `<drive>` — a string that will be trimmed<br>`<targetFolderPath>` — a string that will be trimmed | — | `--path <value>` — a string that will be trimmed<br>`--item-id <value>` — a string that will be trimmed |
| `huly drive items rename <drive> <title>` | Rename Drive Item | `<drive>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed | — | `--path <value>` — a string that will be trimmed<br>`--item-id <value>` — a string that will be trimmed |
| `huly drive list` | List Drives | — | — | `--query <value>` — a string that will be trimmed<br>`--include-archived` / `--no-include-archived` — Include archived Drives. Defaults to false.<br>`--limit <value>` — Maximum drives to return (default: 50). |
| `huly drive members add <drive> <members>` | Add drive members; members is a JSON array Requires `--yes`. | `<drive>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | — |
| `huly drive members remove <drive> <members>` | Remove drive members; members is a JSON array Requires `--yes`. | `<drive>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | — |
| `huly drive owners set <drive> <owners>` | Replace drive owners; owners is a JSON array Requires `--yes`. | `<drive>` — a string that will be trimmed<br>`<owners>` — Replacement Drive owner list. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear owners. Pass arrays or objects as JSON. | — | `--ensure-members` / `--no-ensure-members` — Also add each owner to Drive members. Defaults to true. |
| `huly drive update <drive>` | Update Drive | `<drive>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New plain-text Drive description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the Drive is private.<br>`--archived` / `--no-archived` — Whether the Drive is archived.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the Drive.<br>`--description-file <path>` — read description as text |
| `huly inventory categories list` | List Inventory Categories | — | — | `--query <value>` — a string that will be trimmed<br>`--parent-category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of categories to return (default: 50). |
| `huly inventory category create <name>` | Create Inventory Category | `<name>` — a string that will be trimmed | — | `--parent-category <value>` — a string that will be trimmed |
| `huly inventory category delete <category>` | Delete Inventory Category Requires `--yes`. | `<category>` — a string that will be trimmed | — | `--parent-category <value>` — a string that will be trimmed |
| `huly inventory category get <category>` | Get Inventory Category | `<category>` — a string that will be trimmed | — | `--parent-category <value>` — a string that will be trimmed |
| `huly inventory category update <category>` | Update Inventory Category | `<category>` — a string that will be trimmed | — | `--parent-category <value>` — a string that will be trimmed<br>`--name <value>` — a string that will be trimmed<br>`--new-parent-category <value>` — a string that will be trimmed |
| `huly inventory product activity list <product>` | List Inventory Product Activity | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly inventory product attachment delete <product> <attachmentId>` | Delete Inventory Product Attachment Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. |
| `huly inventory product attachment get <product> <attachmentId>` | Get Inventory Product Attachment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. |
| `huly inventory product attachment update <product> <attachmentId>` | Update Inventory Product Attachment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<attachmentId>` — Product attachment ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--description <value>` — Optional media description. Use null on update to clear it. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned.<br>`--description-file <path>` — read description as text |
| `huly inventory product attachments list <product>` | List Inventory Product Attachments | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly inventory product comment add <product> <body>` | Add Inventory Product Comment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--body-file <path>` — read body as text |
| `huly inventory product comment delete <product> <commentId>` | Delete Inventory Product Comment Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<commentId>` — Product comment ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. |
| `huly inventory product comment update <product> <commentId> <body>` | Update Inventory Product Comment | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<commentId>` — Product comment ID. Must belong directly to the resolved inventory product.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--body-file <path>` — read body as text |
| `huly inventory product comments list <product>` | List Inventory Product Comments | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly inventory product create <name> <category>` | Create Inventory Product | `<name>` — a string that will be trimmed<br>`<category>` — a string that will be trimmed | — | — |
| `huly inventory product delete <product>` | Delete Inventory Product Requires `--yes`. | `<product>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed |
| `huly inventory product get <product>` | Get Inventory Product | `<product>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed |
| `huly inventory product photo delete <product> <photoId>` | Delete Inventory Product Photo Requires `--yes`. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. |
| `huly inventory product photo get <product> <photoId>` | Get Inventory Product Photo | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names. |
| `huly inventory product photo update <product> <photoId>` | Update Inventory Product Photo | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<photoId>` — Product photo ID. Must belong directly to the resolved inventory product. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--description <value>` — Optional media description. Use null on update to clear it. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned.<br>`--description-file <path>` — read description as text |
| `huly inventory product photos list <product>` | List Inventory Product Photos | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly inventory product update <product>` | Update Inventory Product | `<product>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed<br>`--name <value>` — a string that will be trimmed<br>`--new-category <value>` — a string that will be trimmed |
| `huly inventory products attachments add <product> <filename> <contentType>` | Add a product attachment. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<filename>` — Name of the file to attach to the inventory product.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional media description. Use null on update to clear it.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly inventory products list` | List Inventory Products | — | — | `--query <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of products to return (default: 50). |
| `huly inventory products photos add <product> <filename> <contentType>` | Add a product photo. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<product>` — Inventory product ID or exact product name. Pass category when duplicate product names may exist.<br>`<filename>` — Name of the file to attach to the inventory product.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate duplicate product names.<br>`--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional media description. Use null on update to clear it.<br>`--pinned` / `--no-pinned` — Whether the media item should be pinned.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly inventory variant create <product> <name> <sku>` | Create Inventory Variant | `<product>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed<br>`<sku>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed |
| `huly inventory variant delete <variant>` | Delete Inventory Variant Requires `--yes`. | `<variant>` — a string that will be trimmed | — | `--product <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed |
| `huly inventory variant get <variant>` | Get Inventory Variant | `<variant>` — a string that will be trimmed | — | `--product <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed |
| `huly inventory variant update <variant>` | Update Inventory Variant | `<variant>` — a string that will be trimmed | — | `--product <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed<br>`--name <value>` — a string that will be trimmed<br>`--sku <value>` — a string that will be trimmed |
| `huly inventory variants list` | List Inventory Variants | — | — | `--query <value>` — a string that will be trimmed<br>`--product <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of variants to return (default: 50). |
| `huly issue-statuses create <name> <category>` | Create Issue Status | `<name>` — a string that will be trimmed<br>`<category>` | — | `--project-type <value>` — a string that will be trimmed<br>`--task-type <value>` — a string that will be trimmed |
| `huly issues component set` | Set or clear an issue component | — | `--project <value>` — a string that will be trimmed<br>`--identifier <value>` — a string that will be trimmed<br>`--component <value>` — Component ID or label (null to clear) Pass null to clear the field. | — |
| `huly issues create` | Create an issue | — | `--project <value>` — a string that will be trimmed<br>`--title <value>` — a string that will be trimmed | `--description <value>` — Issue description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references. External URLs and other-workspace browse URLs stay normal links.<br>`--priority <value>`<br>`--assignee <value>` — Assignee email address or display name<br>`--status <value>` — a string that will be trimmed<br>`--task-type <value>` — a string that will be trimmed<br>`--parent-issue <value>` — a string that will be trimmed<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds (e.g., 1719792000000 for 2024-07-01), or null to clear Pass null to clear the field.<br>`--estimation <value>` — Time estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day.<br>`--description-file <path>` — read description as text |
| `huly issues delete <project> <identifier>` | Delete Issue Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | — |
| `huly issues documents link` | Link a document to an issue Requires `--yes`. | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed | — |
| `huly issues documents unlink` | Unlink a document from an issue Requires `--yes`. | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed | — |
| `huly issues from-template create <project> <template>` | Create Issue From Template | `<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — Override description in markdown (uses template description if not specified). Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--priority <value>`<br>`--assignee <value>` — Override assignee email or display name<br>`--status <value>` — a string that will be trimmed<br>`--include-children` / `--no-include-children` — Whether to create sub-issues from template children (default: true)<br>`--description-file <path>` — read description as text |
| `huly issues get <project> <identifier>` | Get an issue | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | — |
| `huly issues labels add` | Add an issue label | — | `--project <value>` — a string that will be trimmed<br>`--identifier <value>` — a string that will be trimmed<br>`--label <value>` — a string that will be trimmed | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0) |
| `huly issues labels remove` | Remove an issue label | — | `--project <value>` — a string that will be trimmed<br>`--identifier <value>` — a string that will be trimmed<br>`--label <value>` — a string that will be trimmed | — |
| `huly issues list` | List issues | — | `--project <value>` — a string that will be trimmed | `--status <value>` — a string that will be trimmed<br>`--status-category <value>`<br>`--assignee <value>` — Filter by assignee email or display name<br>`--creator <value>` — Filter by creator using a raw Person ID, exact email address, or exact display name. Ambiguous exact display names are rejected; unknown people return an empty list.<br>`--parent-issue <value>` — a string that will be trimmed<br>`--title-search <value>` — Search issues by title substring (case-insensitive). Mutually exclusive with titleRegex.<br>`--title-regex <value>` — Filter issues by title using Huly $regex. On the supported Postgres backend this is SQL SIMILAR TO, not JavaScript RegExp; matching is case-sensitive and the pattern must match the whole title: use '%' for any string (e.g., '%BUG%' contains, 'BUG%' prefix). Mutually exclusive with titleSearch; use titleSearch for simple substring matching.<br>`--description-search <value>` — Search issues by description content (fulltext search)<br>`--component <value>` — a string that will be trimmed<br>`--label <value>` — a string that will be trimmed<br>`--milestone <value>` — Milestone ID or exact project-scoped label after trimming and case-insensitive matching. Mutually exclusive with hasMilestone.<br>`--has-milestone` / `--no-has-milestone` — Filter by milestone presence. true = only scheduled issues, false = only issues without a milestone.<br>`--has-assignee` / `--no-has-assignee` — Filter by assignee presence. true = only assigned issues, false = only unassigned issues.<br>`--has-due-date` / `--no-has-due-date` — Filter by due date presence. true = only issues with a due date, false = only issues without.<br>`--has-component` / `--no-has-component` — Filter by component presence. true = only issues with a component, false = only issues without.<br>`--is-top-level` / `--no-is-top-level` — When true, only return top-level issues (not sub-issues). false or omitted returns all issues.<br>`--limit <value>` — Maximum number of issues to return (default: 50) |
| `huly issues milestone set` | Set or clear an issue milestone | — | `--project <value>` — a string that will be trimmed<br>`--identifier <value>` — a string that will be trimmed<br>`--milestone <value>` — Milestone ID or label (null to clear) Pass null to clear the field. | — |
| `huly issues move <project> <identifier>` | Move an issue | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | `--new-parent <value>` — New parent issue identifier, or null to restore the native top-level issue shape Pass null to clear the field. | — |
| `huly issues related-targets delete <space>` | Delete Related Issue Space Target Requires `--yes`. | `<space>` — a string that will be trimmed | — | — |
| `huly issues related-targets list` | List Related Issue Targets | — | — | `--space <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of related issue targets to return (default: 50). |
| `huly issues related-targets set <targetProject>` | Set Related Issue Target | `<targetProject>` — Default destination project identifier for related issues from the selected space or object class, or null to clear the default destination project. Pass null to clear the field. | — | `--space <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed |
| `huly issues relations add` | Add an issue relation | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--target-issue <value>` — a string that will be trimmed<br>`--relation-type <value>` | — |
| `huly issues relations list` | List issue relations | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed | — |
| `huly issues relations remove` | Remove an issue relation | — | `--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--target-issue <value>` — a string that will be trimmed<br>`--relation-type <value>` | — |
| `huly issues templates children add <title> <project> <template>` | Add Template Child | `<title>` — a string that will be trimmed<br>`<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed | — | `--description <value>` — Child template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--priority <value>`<br>`--assignee <value>` — Child default assignee email or display name<br>`--component <value>` — a string that will be trimmed<br>`--estimation <value>` — Child default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day.<br>`--description-file <path>` — read description as text |
| `huly issues templates children remove <project> <template> <childId>` | Remove Template Child Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed<br>`<childId>` — a string that will be trimmed | — | — |
| `huly issues templates create <project> <title>` | Create Issue Template | `<project>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed | — | `--description <value>` — Template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--priority <value>`<br>`--assignee <value>` — Default assignee email address or display name<br>`--component <value>` — a string that will be trimmed<br>`--estimation <value>` — Default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day.<br>`--children <value>` — Child (sub-task) templates to include Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly issues templates delete <project> <template>` | Delete Issue Template Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed | — | — |
| `huly issues templates get <project> <template>` | Get Issue Template | `<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed | — | — |
| `huly issues templates list <project>` | List Issue Templates | `<project>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of templates to return (default: 50) |
| `huly issues templates update <project> <template>` | Update Issue Template | `<project>` — a string that will be trimmed<br>`<template>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — New template description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--priority <value>`<br>`--assignee <value>` — New default assignee email or display name (null to unassign) Pass null to clear the field.<br>`--component <value>` — New default component ID or label (null to clear) Pass null to clear the field.<br>`--estimation <value>` — New default estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Use null to clear. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly issues update <project> <identifier>` | Update an issue | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — New issue description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references. External URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--priority <value>`<br>`--assignee <value>` — New assignee email or display name (null to unassign) Pass null to clear the field.<br>`--status <value>` — a string that will be trimmed<br>`--task-type <value>` — a string that will be trimmed<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds (e.g., 1719792000000 for 2024-07-01), or null to clear Pass null to clear the field.<br>`--estimation <value>` — Time estimation in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. Use null to clear. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly labels create <title>` | Create Label | `<title>` — a string that will be trimmed | — | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0)<br>`--description <value>` — Label description<br>`--category <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly labels delete <label>` | Delete Label Requires `--yes`. | `<label>` — a string that will be trimmed | — | — |
| `huly labels list` | List labels | — | — | `--category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of labels to return (default: 50) |
| `huly labels update <label>` | Update Label | `<label>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--color <value>` — New Huly platform color palette index from 0 through 23<br>`--description <value>` — New label description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly leads create <funnel> <customer> <title>` | Create Lead | `<funnel>` — a string that will be trimmed<br>`<customer>` — Existing person or organization to attach as the customer. The contact is promoted to a Huly Customer idempotently when needed. Pass arrays or objects as JSON.<br>`<title>` — a string that will be trimmed | — | `--description <value>` — Optional Markdown description. Current-workspace Huly links are preserved as native references.<br>`--assignee <value>` — Optional employee assignee by person/employee ID, exact email, or exact display name.<br>`--status <value>` — a string that will be trimmed<br>`--task-type <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly leads funnels list` | List Funnels | — | — | `--include-archived` / `--no-include-archived` — Include archived funnels in results (default: false, showing only active)<br>`--limit <value>` — Maximum number of funnels to return (default: 50) |
| `huly leads get <funnel> <identifier>` | Get Lead | `<funnel>` — a string that will be trimmed<br>`<identifier>` | — | — |
| `huly leads list <funnel>` | List Leads | `<funnel>` — a string that will be trimmed | — | `--status <value>` — a string that will be trimmed<br>`--assignee <value>` — Filter by assignee email or display name<br>`--title-search <value>` — Search leads by title substring (case-insensitive)<br>`--limit <value>` — Maximum number of leads to return (default: 50) |
| `huly mail threads list` | List Mail Threads | — | — | `--space <value>` — a string that will be trimmed<br>`--channel-title-search <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of Mail thread channels to return (default: 50) |
| `huly milestones create <project> <label> <targetDate>` | Create Milestone | `<project>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed<br>`<targetDate>` — Target date as Unix timestamp in milliseconds | — | `--description <value>` — Milestone description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--description-file <path>` — read description as text |
| `huly milestones delete <project> <milestone>` | Delete Milestone Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<milestone>` — a string that will be trimmed | — | — |
| `huly milestones get` | Get a milestone | — | `--project <value>` — a string that will be trimmed<br>`--milestone <value>` — a string that will be trimmed | — |
| `huly milestones list` | List milestones | — | `--project <value>` — a string that will be trimmed | `--limit <value>` — Maximum number of milestones to return (default: 50) |
| `huly milestones update <project> <milestone>` | Update Milestone | `<project>` — a string that will be trimmed<br>`<milestone>` — a string that will be trimmed | — | `--label <value>` — a string that will be trimmed<br>`--description <value>` — New milestone description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--target-date <value>` — New target date as Unix timestamp in milliseconds<br>`--status <value>` — New milestone status<br>`--description-file <path>` — read description as text |
| `huly model attributes create <class> <name> <label>` | Create Huly Model Attribute | `<class>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed<br>`<label>` — a string that will be trimmed | `--type <value>` — Pass arrays or objects as JSON.<br>`--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | `--index <value>`<br>`--automation-only` / `--no-automation-only`<br>`--hidden` / `--no-hidden` |
| `huly model attributes delete <attribute>` | Delete Huly Model Attribute Requires `--yes`. | `<attribute>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | `--class <value>` — a string that will be trimmed |
| `huly model attributes list` | List Huly model attributes | — | — | `--class <value>` — a string that will be trimmed<br>`--query <value>` — a string that will be trimmed<br>`--custom-only` / `--no-custom-only` — Only return attributes marked as custom fields. Defaults to false.<br>`--limit <value>` — Maximum number of attributes to return after filtering (default: 100, max: 200) |
| `huly model attributes update <attribute>` | Update Huly Model Attribute | `<attribute>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | `--class <value>` — a string that will be trimmed<br>`--label <value>` — a string that will be trimmed<br>`--index <value>` — Pass null to clear the field.<br>`--automation-only` / `--no-automation-only`<br>`--hidden` / `--no-hidden` |
| `huly model classes get <class>` | Get a Huly model class and its attributes | `<class>` — a string that will be trimmed | — | `--include-inherited-attributes` / `--no-include-inherited-attributes` — Include attributes declared on parent classes. Defaults to true. |
| `huly model classes list` | List Huly model classes, interfaces, and mixins | — | — | `--query <value>` — a string that will be trimmed<br>`--kind <value>` — Filter by class, interface, or mixin. unknown is only returned for unexpected model values.<br>`--domain <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of classes to return after filtering (default: 100, max: 200) |
| `huly model collaborators delete <class>` | Delete Class Collaborator Metadata Requires `--yes`. | `<class>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | — |
| `huly model collaborators get <class>` | Get Class Collaborator Metadata | `<class>` — a string that will be trimmed | — | — |
| `huly model collaborators set <class> <fieldSelection>` | Set Class Collaborator Metadata Requires `--yes`. | `<class>` — a string that will be trimmed<br>`<fieldSelection>` — Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | `--provide-security` / `--no-provide-security` — Propagate security through collaborator fields; defaults to false.<br>`--provide-attached-security` / `--no-provide-attached-security` — Propagate security through attached collaborator documents; defaults to false. |
| `huly model domain-indexes list` | List Huly domain index configurations | — | — | — |
| `huly model enums create <name> <values>` | Create Huly Model Enum | `<name>` — a string that will be trimmed<br>`<values>` — Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | — |
| `huly model enums delete <enum>` | Delete Huly Model Enum Requires `--yes`. | `<enum>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | — |
| `huly model enums list` | List Huly model enums | — | — | `--enum <value>` — a string that will be trimmed<br>`--query <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of enums to return after filtering (default: 100, max: 200) |
| `huly model enums update <enum>` | Update Huly Model Enum | `<enum>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes Huly workspace model metadata. | `--name <value>` — a string that will be trimmed<br>`--values <value>` — Pass arrays or objects as JSON. |
| `huly model permissions create <label> <scope>` | Create Huly Permission Requires `--yes`. | `<label>` — a string that will be trimmed<br>`<scope>` | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | `--object-class <value>` — a string that will be trimmed<br>`--transaction <value>` — Optional transaction kind constrained by this permission: create, update, remove, or mixin.<br>`--forbid` / `--no-forbid`<br>`--description <value>` — a string that will be trimmed |
| `huly model permissions delete <permission>` | Delete Huly Permission Requires `--yes`. | `<permission>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | — |
| `huly model permissions update <permission>` | Update Huly Permission Requires `--yes`. | `<permission>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | `--label <value>` — a string that will be trimmed<br>`--scope <value>`<br>`--object-class <value>` — Pass null to clear the field.<br>`--transaction <value>` — Pass null to clear the field.<br>`--forbid` / `--no-forbid`<br>`--description <value>` — Pass null to clear the field. |
| `huly model plugins list` | List Huly plugin configurations | — | — | — |
| `huly model sequences create <class> <kind>` | Create Huly Sequence | `<class>` — a string that will be trimmed<br>`<kind>` | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. | `--prefix <value>` — a string that will be trimmed |
| `huly model sequences delete <sequence> <expectedCurrentValue>` | Delete Huly Sequence Requires `--yes`. | `<sequence>` — Exact sequence ID or attached class ID/name. Resolution tries sequence ID first, then class.<br>`<expectedCurrentValue>` — Must be 0. Only a never-used sequence can be deleted, and deletion is refused if its counter advanced. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. | — |
| `huly model sequences list` | List Huly model sequences | — | — | — |
| `huly model sequences update <sequence> <prefix>` | Update Huly Custom Sequence | `<sequence>` — Exact sequence ID or attached class ID/name. Resolution tries sequence ID first, then class.<br>`<prefix>` — a string that will be trimmed | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes a Huly sequence definition. | — |
| `huly model space-types capabilities describe <spaceType>` | Describe a Huly space type's model capabilities | `<spaceType>` — a string that will be trimmed | — | — |
| `huly notifications all archive` | Archive all notifications for the current account Requires `--yes`. | — | — | — |
| `huly notifications all read` | Mark all notifications read for the current account Requires `--yes`. | — | — | — |
| `huly notifications archive <notificationId>` | Archive Notification | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications contexts archive <contextId>` | Archive Notification Context | `<contextId>` — a string that will be trimmed | — | — |
| `huly notifications contexts get <objectId> <objectClass>` | Get Notification Context | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed | — | — |
| `huly notifications contexts hide <contextId> <hidden>` | Hide Notification Context Requires `--yes`. | `<contextId>` — a string that will be trimmed<br>`<hidden>` — Whether to hide (true) or unhide (false) the context | — | — |
| `huly notifications contexts list` | List Notification Contexts | — | — | `--limit <value>` — Maximum number of contexts to return (default: 50)<br>`--pinned-only` / `--no-pinned-only` — Return only pinned contexts (default: false)<br>`--include-hidden` / `--no-include-hidden` — Include hidden notification contexts in results (default: false) |
| `huly notifications contexts pin <contextId> <pinned>` | Pin Notification Context | `<contextId>` — a string that will be trimmed<br>`<pinned>` — Whether to pin (true) or unpin (false) the context | — | — |
| `huly notifications contexts unarchive <contextId>` | Unarchive Notification Context Requires `--yes`. | `<contextId>` — a string that will be trimmed | — | — |
| `huly notifications delete <notificationId>` | Delete Notification Requires `--yes`. | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications get <notificationId>` | Get Notification | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications list` | List Notifications | — | — | `--limit <value>` — Maximum number of notifications to return (default: 50)<br>`--include-archived` / `--no-include-archived` — Include archived notifications in results (default: false)<br>`--unread-only` / `--no-unread-only` — Return only unread notifications (default: false) |
| `huly notifications providers list` | List Notification Providers | — | — | `--limit <value>` — Maximum number of providers to return (default: 50)<br>`--include-unavailable` / `--no-include-unavailable` — Include providers that the workspace may not currently expose as configurable settings. |
| `huly notifications providers settings update <providerId> <enabled>` | Update Notification Provider Setting | `<providerId>` — a string that will be trimmed<br>`<enabled>` — Whether to enable or disable the provider | — | — |
| `huly notifications read mark <notificationId>` | Mark Notification Read | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications settings list` | List Notification Settings | — | — | `--limit <value>` — Maximum number of settings to return (default: 50) |
| `huly notifications types list` | List Notification Types | — | — | `--limit <value>` — Maximum number of notification types to return (default: 50)<br>`--include-hidden` / `--no-include-hidden` — Include hidden/internal notification types.<br>`--object-class <value>` — a string that will be trimmed |
| `huly notifications types settings update <providerId> <typeId> <enabled>` | Update Notification Type Setting | `<providerId>` — a string that will be trimmed<br>`<typeId>` — a string that will be trimmed<br>`<enabled>` — Whether to enable or disable this notification type for the provider. | — | — |
| `huly notifications unarchive <notificationId>` | Unarchive Notification Requires `--yes`. | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications unread mark <notificationId>` | Mark Notification Unread | `<notificationId>` — a string that will be trimmed | — | — |
| `huly notifications unread-count get` | Get Unread Notification Count | — | — | — |
| `huly objects collaborators add <member>` | Add a collaborator using one raw, issue, or document object locator Requires `--yes`. | `<member>` | — | `--object-id <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed<br>`--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed |
| `huly objects collaborators remove <member>` | Remove a collaborator using one raw, issue, or document object locator Requires `--yes`. | `<member>` | — | `--object-id <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed<br>`--project <value>` — a string that will be trimmed<br>`--issue-identifier <value>` — a string that will be trimmed<br>`--teamspace <value>` — a string that will be trimmed<br>`--document <value>` — a string that will be trimmed |
| `huly objects custom-fields set <objectId> <objectClass> <fieldId> <value>` | Set a custom field on a raw Huly object | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed<br>`<fieldId>` — a string that will be trimmed<br>`<value>` — Value to set. Strings are passed as-is. For numbers, pass a numeric string (e.g. '42'). For dates, pass a real ISO calendar date in YYYY-MM-DD form (stored as UTC midnight) or a canonical non-negative epoch-millisecond string from 0 through 8640000000000000. Date-times, time-zone suffixes, signs, whitespace, decimals, and exponents are rejected. For booleans, pass 'true' or 'false'. For enums, pass the enum value string. | — | — |
| `huly objects notifications subscribe <objectId> <objectClass>` | Subscribe the current account to raw-object notifications Requires `--yes`. | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed | — | `--space <value>` — a string that will be trimmed |
| `huly objects notifications unsubscribe <objectId> <objectClass>` | Unsubscribe the current account from raw-object notifications Requires `--yes`. | `<objectId>` — a string that will be trimmed<br>`<objectClass>` — a string that will be trimmed | — | `--space <value>` — a string that will be trimmed |
| `huly objects tags attach <targetClass> <tag>` | Attach a tag; pass the raw target locator with --object as JSON Requires `--yes`. | `<targetClass>` — a string that will be trimmed<br>`<tag>` — a string that will be trimmed | `--object <value>` — Raw SDK object locator for tag references. Use module-specific wrapper tools when available; this locator is for SDK parity. Pass arrays or objects as JSON. | `--color <value>` — Huly platform color palette index from 0 through 23 for a newly created tag definition (default: 0). Ignored when the tag already exists.<br>`--category <value>` — a string that will be trimmed<br>`--weight <value>` — Optional weight/knowledge level to store on the TagReference. |
| `huly objects tags detach <targetClass> <tag>` | Detach a tag; pass the raw target locator with --object as JSON Requires `--yes`. | `<targetClass>` — a string that will be trimmed<br>`<tag>` — a string that will be trimmed | `--object <value>` — Raw SDK object locator for tag references. Use module-specific wrapper tools when available; this locator is for SDK parity. Pass arrays or objects as JSON. | — |
| `huly office defaults list` | List Office Defaults | — | — | — |
| `huly office devices preferences list` | List Device Preferences | — | — | — |
| `huly office floors get <floorId>` | Get Office Floor | `<floorId>` — a string that will be trimmed | — | — |
| `huly office floors list` | List Office Floors | — | — | `--limit <value>` — Maximum number of floors to return (default: 50). |
| `huly office meeting-minutes get <meetingMinutesId>` | Get Meeting Minutes | `<meetingMinutesId>` — a string that will be trimmed | — | — |
| `huly office meeting-minutes list` | List Meeting Minutes | — | — | `--attached-to-id <value>` — a string that will be trimmed<br>`--from <value>` — Created-on lower bound timestamp.<br>`--to <value>` — Created-on upper bound timestamp.<br>`--limit <value>` — Maximum number of meeting notes/transcript records (minutes) to return (default: 50). |
| `huly office offices get <roomId>` | Get Office | `<roomId>` — a string that will be trimmed | — | — |
| `huly office offices list` | List Offices | — | — | `--floor-id <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of rooms to return (default: 50). |
| `huly office rooms active-info list` | List Active Room Info | — | — | — |
| `huly office rooms get <roomId>` | Get Office Room | `<roomId>` — a string that will be trimmed | — | — |
| `huly office rooms list` | List Office Rooms | — | — | `--floor-id <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of rooms to return (default: 50). |
| `huly office rooms participants list` | List Active Room Participants | — | — | `--room-id <value>` — a string that will be trimmed |
| `huly planner todos complete <locator>` | Complete Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | `--done-on <value>` — Completion timestamp. If omitted, uses the current time. |
| `huly planner todos create <title>` | Create Todo | `<title>` — a string that will be trimmed | — | `--description <value>` — ToDo description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--owner <value>` — a string that will be trimmed<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds.<br>`--priority <value>` — Priority. Default: no-priority.<br>`--visibility <value>` — Visibility. Default: private for personal ToDos, public for issue ToDos.<br>`--attached-to <value>` — Attachment target. If omitted, creates a personal ToDo. Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly planner todos delete <locator>` | Delete Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | — |
| `huly planner todos get <locator>` | Get Todo | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | — |
| `huly planner todos labels add <locator> <label>` | Add Todo Label | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON.<br>`<label>` — a string that will be trimmed | — | `--color <value>` — Color for a newly created label definition from 0 through 23 (default: 0). Ignored for an existing label. |
| `huly planner todos labels definitions list` | List Todo Label Definitions | — | — | `--title-search <value>` — Optional label title substring search.<br>`--limit <value>` — Maximum number of label definitions to return (default: 50). |
| `huly planner todos labels list <locator>` | List Todo Labels | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | — |
| `huly planner todos labels remove <locator> <label>` | Remove Todo Label Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON.<br>`<label>` — a string that will be trimmed | — | — |
| `huly planner todos list` | List Todos | — | — | `--owner <value>` — a string that will be trimmed<br>`--issue <value>` — Filter ToDos attached to one issue. Pass arrays or objects as JSON.<br>`--title <value>` — a string that will be trimmed<br>`--title-search <value>` — a string that will be trimmed<br>`--due-from <value>` — Only ToDos due at or after this timestamp.<br>`--due-to <value>` — Only ToDos due at or before this timestamp.<br>`--completion-state <value>` — Completion filter. Default: all.<br>`--priority <value>` — Planner ToDo priority. Allowed values: no-priority, low, medium, high, urgent.<br>`--visibility <value>` — Planner ToDo visibility. Allowed values: public, freeBusy, private.<br>`--limit <value>` — Maximum number of ToDos to return (default: 50). |
| `huly planner todos reopen <locator>` | Reopen Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | — |
| `huly planner todos schedule <locator> <date> <dueDate>` | Schedule Todo Requires `--yes`. | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON.<br>`<date>` — Work slot start timestamp.<br>`<dueDate>` — Work slot end timestamp. | — | — |
| `huly planner todos unschedule` | Unschedule Todo Requires `--yes`. | — | — | `--work-slot-id <value>` — a string that will be trimmed<br>`--locator <value>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON.<br>`--scope <value>` — Remove future work slots for the located ToDo.<br>`--from <value>` — Reference timestamp for future work slots. If omitted, uses current time. |
| `huly planner todos update <locator>` | Update Todo | `<locator>` — LLM-first ToDo locator. Prefer issue/title/owner forms when you do not know the raw Huly ToDo ID. Pass arrays or objects as JSON. | — | `--title <value>` — a string that will be trimmed<br>`--description <value>` — New ToDo description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--owner <value>` — a string that will be trimmed<br>`--due-date <value>` — New due date timestamp, or null to clear. Pass null to clear the field.<br>`--priority <value>` — Planner ToDo priority. Allowed values: no-priority, low, medium, high, urgent.<br>`--visibility <value>` — Planner ToDo visibility. Allowed values: public, freeBusy, private.<br>`--description-file <path>` — read description as text |
| `huly platform associations list` | List Associations | — | — | `--association <value>` — a string that will be trimmed<br>`--source-class <value>` — a string that will be trimmed<br>`--target-class <value>` — a string that will be trimmed<br>`--writable-only` / `--no-writable-only` — Only return associations whose relation create/delete path has been validated and allowlisted<br>`--include-system` / `--no-include-system` — Include internal/system associations. Defaults to false.<br>`--limit <value>` — Maximum number of associations to return (default: 50) |
| `huly platform relations list` | List Relations | — | — | `--association <value>` — a string that will be trimmed<br>`--source <value>` — Optional source endpoint filter Pass arrays or objects as JSON.<br>`--target <value>` — Optional target endpoint filter Pass arrays or objects as JSON.<br>`--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target.<br>`--limit <value>` — Maximum number of relations to return (default: 50) |
| `huly preferences spaces get <space>` | Get Space Preference | `<space>` — a string that will be trimmed | — | `--include-archived` / `--no-include-archived` — Allow matching archived spaces by exact name. ID lookup can return archived spaces.<br>`--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly preferences spaces list` | List Space Preferences | — | — | `--space <value>` — a string that will be trimmed<br>`--include-archived` / `--no-include-archived` — Allow matching archived spaces by exact name when space is provided. ID lookup can return archived spaces.<br>`--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of space preferences to return (default: 50). |
| `huly processes executions cancel <execution>` | Cancel an active process execution Requires `--yes`. | `<execution>` — a string that will be trimmed | — | — |
| `huly processes executions list` | List Process Executions | — | — | `--process <value>` — a string that will be trimmed<br>`--card <value>` — a string that will be trimmed<br>`--status <value>` — Optional execution status filter: active, done, or cancelled.<br>`--limit <value>` — Maximum number of executions to return (default: 50, maximum: 200). |
| `huly processes get <process>` | Get Process | `<process>` — a string that will be trimmed | — | — |
| `huly processes list` | List Processes | — | — | `--master-tag <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of process definitions to return (default: 50, maximum: 200). |
| `huly processes start <process> <card>` | Start a process on a card or document Requires `--yes`. | `<process>` — a string that will be trimmed<br>`<card>` — a string that will be trimmed | — | — |
| `huly project-types get` | Get Project Type | — | — | `--project-type <value>` — a string that will be trimmed |
| `huly project-types list` | List Project Types | — | — | — |
| `huly projects create <name> <identifier>` | Create Project | `<name>` — a string that will be trimmed<br>`<identifier>` — Unique project identifier, 1-5 uppercase alphanumeric chars starting with letter (e.g., 'HULY', 'QA') | — | `--description <value>` — Project description<br>`--private` / `--no-private` — Whether project is private (default: false)<br>`--description-file <path>` — read description as text |
| `huly projects delete <project>` | Delete Project Requires `--yes`. | `<project>` — a string that will be trimmed | — | — |
| `huly projects get <project>` | Get a project | `<project>` — a string that will be trimmed | — | — |
| `huly projects list` | List projects | — | — | `--include-archived` / `--no-include-archived` — Include archived projects in results (default: false, showing only active)<br>`--limit <value>` — Maximum number of projects to return (default: 50) |
| `huly projects statuses <project>` | List project issue statuses | `<project>` — a string that will be trimmed | — | — |
| `huly projects target-preferences list` | List Project Target Preferences | — | — | `--project <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of preferences to return (default: 50). |
| `huly projects target-preferences upsert <project>` | Create or update a project target preference; pass --props as a JSON array Requires `--yes`. | `<project>` — a string that will be trimmed | — | `--props <value>` — Optional SDK-open target preference props to merge by key. Existing keys are replaced; other existing props are preserved. Pass arrays or objects as JSON. |
| `huly projects update <project>` | Update Project | `<project>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New description (null to clear) Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting activity list <target>` | List Recruiting Activity | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly recruiting applicant create <vacancy> <candidate>` | Create Recruiting Applicant | `<vacancy>`<br>`<candidate>` — a string that will be trimmed | — | `--status <value>` — a string that will be trimmed<br>`--assignee <value>`<br>`--start-date <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--due-date <value>` — Unix timestamp in milliseconds (non-negative integer) |
| `huly recruiting applicant delete <applicant>` | Delete Recruiting Applicant Requires `--yes`. | `<applicant>` | — | `--vacancy <value>`<br>`--candidate <value>` — a string that will be trimmed |
| `huly recruiting applicant get <applicant>` | Get Recruiting Applicant | `<applicant>` | — | `--vacancy <value>`<br>`--candidate <value>` — a string that will be trimmed |
| `huly recruiting applicant match get <match>` | Get Recruiting Applicant Match | `<match>` — a string that will be trimmed | — | — |
| `huly recruiting applicant matches list` | List Recruiting Applicant Matches | — | — | `--candidate <value>` — a string that will be trimmed<br>`--complete` / `--no-complete` — Filter generated applicant matches by Huly completion state.<br>`--query <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of applicant matches to return (default: 50). |
| `huly recruiting applicant update <applicant>` | Update Recruiting Applicant | `<applicant>` | — | `--vacancy <value>`<br>`--candidate <value>` — a string that will be trimmed<br>`--status <value>` — a string that will be trimmed<br>`--assignee <value>` — Pass null to clear the field.<br>`--start-date <value>` — Pass null to clear the field.<br>`--due-date <value>` — Pass null to clear the field. |
| `huly recruiting applicants list` | List Recruiting Applicants | — | — | `--vacancy <value>`<br>`--candidate <value>` — a string that will be trimmed<br>`--status <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of applicants to return (default: 50). |
| `huly recruiting attachment delete <target> <attachmentId>` | Delete Recruiting Attachment Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. | — | — |
| `huly recruiting attachment get <target> <attachmentId>` | Get Recruiting Attachment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. | — | — |
| `huly recruiting attachment update <target> <attachmentId>` | Update Recruiting Attachment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<attachmentId>` — Attachment ID. Must belong directly to the resolved Recruiting target. | — | `--description <value>` — Optional attachment description. Use null on update to clear it. Pass null to clear the field.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned.<br>`--description-file <path>` — read description as text |
| `huly recruiting attachments add <target> <filename> <contentType>` | Add a recruiting attachment. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<filename>` — Name of the file to attach to the Recruiting object.<br>`<contentType>` — MIME type of the file, such as image/png or application/pdf. | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--description <value>` — Optional attachment description. Use null on update to clear it.<br>`--pinned` / `--no-pinned` — Whether the attachment should be pinned.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly recruiting attachments list <target>` | List Recruiting Attachments | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly recruiting candidate get <candidate>` | Get Recruiting Candidate | `<candidate>` — a string that will be trimmed | — | — |
| `huly recruiting candidate profile set <candidate>` | Set Recruiting Candidate Profile | `<candidate>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--source <value>` — a string that will be trimmed<br>`--onsite` / `--no-onsite` — Whether the candidate is available for this work mode.<br>`--remote` / `--no-remote` — Whether the candidate is available for this work mode. |
| `huly recruiting candidate skill add <candidate> <skill>` | Add Recruiting Candidate Skill | `<candidate>` — a string that will be trimmed<br>`<skill>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed<br>`--color <value>` — Huly platform color palette index, from 0 through 23.<br>`--weight <value>` — Optional tag reference weight/knowledge level. Kept in exact type-level parity with @hcengineering/tags TagReference.weight. |
| `huly recruiting candidate skill remove <candidate> <skill>` | Remove Recruiting Candidate Skill Requires `--yes`. | `<candidate>` — a string that will be trimmed<br>`<skill>` — a string that will be trimmed | — | — |
| `huly recruiting candidate skills list <candidate>` | List Recruiting Candidate Skills | `<candidate>` — a string that will be trimmed | — | — |
| `huly recruiting candidates list` | List Recruiting Candidates | — | — | `--query <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of candidates to return (default: 50). |
| `huly recruiting comment add <target> <body>` | Add Recruiting Comment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--body-file <path>` — read body as text |
| `huly recruiting comment delete <target> <commentId>` | Delete Recruiting Comment Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<commentId>` — Comment ID. Must belong directly to the resolved Recruiting target. | — | — |
| `huly recruiting comment update <target> <commentId> <body>` | Update Recruiting Comment | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<commentId>` — Comment ID. Must belong directly to the resolved Recruiting target.<br>`<body>` — Comment body in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. | — | `--body-file <path>` — read body as text |
| `huly recruiting comments list <target>` | List Recruiting Comments | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly recruiting opinion create <review> <value>` | Create Recruiting Opinion | `<review>`<br>`<value>` — a string that will be trimmed | — | `--description <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly recruiting opinion delete <opinion>` | Delete Recruiting Opinion Requires `--yes`. | `<opinion>` | — | `--review <value>` |
| `huly recruiting opinion get <opinion>` | Get Recruiting Opinion | `<opinion>` | — | `--review <value>` |
| `huly recruiting opinion update <opinion>` | Update Recruiting Opinion | `<opinion>` | — | `--review <value>`<br>`--value <value>` — a string that will be trimmed<br>`--description <value>` — Non-empty markdown replacement text, or null to clear this rich-text field. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly recruiting opinions list <review>` | List Recruiting Opinions | `<review>` | — | `--limit <value>` — Maximum number of opinions to return (default: 50). |
| `huly recruiting related issue add <target> <issue>` | Add Recruiting Related Issue | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<issue>` — Issue identifier, such as HULY-123, or a numeric issue number when project is also provided. | — | `--project <value>` — Project identifier. Optional when issue already includes a project prefix like HULY-123. |
| `huly recruiting related issue remove <target> <issue>` | Remove Recruiting Related Issue Requires `--yes`. | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON.<br>`<issue>` — Issue identifier, such as HULY-123, or a numeric issue number when project is also provided. | — | `--project <value>` — Project identifier. Optional when issue already includes a project prefix like HULY-123. |
| `huly recruiting related issues list <target>` | List Recruiting Related Issues | `<target>` — Recruiting target locator. Supported kinds depend on the tool: comments use vacancy/candidate/applicant/review/opinion; attachments use vacancy/candidate/applicant/opinion; activity uses vacancy/candidate/applicant/review; related issues use vacancy/candidate/applicant. Pass arrays or objects as JSON. | — | `--limit <value>` — Maximum number of matching rows to return (default: 50). |
| `huly recruiting review create <candidate> <title> <date>` | Create Recruiting Review | `<candidate>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed<br>`<date>` — Unix timestamp in milliseconds (non-negative integer) | — | `--due-date <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--description <value>` — a string that will be trimmed<br>`--verdict <value>` — a string that will be trimmed<br>`--application <value>`<br>`--company <value>` — a string that will be trimmed<br>`--location <value>` — a string that will be trimmed<br>`--participants <value>` — Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly recruiting review delete <review>` | Delete Recruiting Review Requires `--yes`. | `<review>` | — | `--candidate <value>` — a string that will be trimmed<br>`--application <value>` |
| `huly recruiting review get <review>` | Get Recruiting Review | `<review>` | — | `--candidate <value>` — a string that will be trimmed<br>`--application <value>` |
| `huly recruiting review update <review>` | Update Recruiting Review | `<review>` | — | `--candidate <value>` — a string that will be trimmed<br>`--application-context <value>`<br>`--title <value>` — a string that will be trimmed<br>`--description <value>` — Non-empty markdown replacement text, or null to clear this rich-text field. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--verdict <value>` — Non-empty replacement text, or null to clear this field. Pass null to clear the field.<br>`--date <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--due-date <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--application <value>` — Pass null to clear the field.<br>`--company <value>` — Pass null to clear the field.<br>`--location <value>` — Non-empty replacement text, or null to clear this field. Pass null to clear the field.<br>`--participants <value>` — Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly recruiting reviews list` | List Recruiting Reviews | — | — | `--candidate <value>` — a string that will be trimmed<br>`--application <value>`<br>`--query <value>` — a string that will be trimmed<br>`--from <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--to <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--limit <value>` — Maximum number of reviews to return (default: 50). |
| `huly recruiting skills list` | List Recruiting Skills | — | — | `--title-search <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of skills to return (default: 50). |
| `huly recruiting vacancies list` | List Recruiting Vacancies | — | — | `--include-archived` / `--no-include-archived` — Include archived vacancies (default: false).<br>`--query <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--company <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of vacancies to return (default: 50). |
| `huly recruiting vacancy archive <vacancy>` | Archive Recruiting Vacancy | `<vacancy>` | — | — |
| `huly recruiting vacancy create <name>` | Create Recruiting Vacancy | `<name>` — a string that will be trimmed | — | `--short-description <value>` — a string that will be trimmed<br>`--full-description <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--company <value>` — a string that will be trimmed<br>`--location <value>` — a string that will be trimmed<br>`--due-to <value>` — Unix timestamp in milliseconds (non-negative integer)<br>`--private` / `--no-private` — Whether the created or updated Recruiting object should be private.<br>`--short-description-file <path>` — read shortDescription as text<br>`--full-description-file <path>` — read fullDescription as text |
| `huly recruiting vacancy get <vacancy>` | Get Recruiting Vacancy | `<vacancy>` | — | — |
| `huly recruiting vacancy statuses list <vacancy>` | List Recruiting Vacancy Statuses | `<vacancy>` | — | — |
| `huly recruiting vacancy types list` | List Recruiting Vacancy Types | — | — | `--include-archived` / `--no-include-archived` — Include archived vacancy types when Huly marks them archived (default: false).<br>`--limit <value>` — Maximum number of vacancy types to return (default: 50). |
| `huly recruiting vacancy unarchive <vacancy>` | Unarchive Recruiting Vacancy Requires `--yes`. | `<vacancy>` | — | — |
| `huly recruiting vacancy update <vacancy>` | Update Recruiting Vacancy | `<vacancy>` | — | `--name <value>` — a string that will be trimmed<br>`--short-description <value>` — a string that will be trimmed<br>`--full-description <value>` — Non-empty replacement full vacancy description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--type <value>` — a string that will be trimmed<br>`--company <value>` — Replacement company organization ID or exact name, or null to clear. Pass null to clear the field.<br>`--location <value>` — Non-empty replacement vacancy location, or null to clear. Pass null to clear the field.<br>`--due-to <value>` — Pass null to clear the field.<br>`--private` / `--no-private` — Whether the created or updated Recruiting object should be private.<br>`--short-description-file <path>` — read shortDescription as text<br>`--full-description-file <path>` — read fullDescription as text |
| `huly relations create <association>` | Create a relation; pass --source and --target as JSON | `<association>` — a string that will be trimmed | `--source <value>` — Source endpoint document Pass arrays or objects as JSON.<br>`--target <value>` — Target endpoint document Pass arrays or objects as JSON. | `--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target.<br>`--if-exists <value>` — return_existing (default) returns an existing relation; fail reports an existing relation as an error |
| `huly relations delete` | Delete a relation by ID or exact structured endpoints Requires `--yes`. | — | — | `--relation <value>` — a string that will be trimmed<br>`--association <value>` — a string that will be trimmed<br>`--source <value>` — Source endpoint Pass arrays or objects as JSON.<br>`--target <value>` — Target endpoint Pass arrays or objects as JSON.<br>`--direction <value>` — Relation traversal direction: source-to-target, target-to-source, either. Defaults to source-to-target. |
| `huly search <query>` | Search Huly | `<query>` — a string that will be trimmed | — | `--limit <value>` — Maximum number of results to return (default: 50) |
| `huly spaces admins get` | Get Global Space Admins | — | — | — |
| `huly spaces admins set` | Set Global Space Admins Requires `--yes`. | — | `--admins <value>` — Complete replacement global space-admin list. Accepts account UUIDs, exact emails, or exact person display names; pass [] to clear the role. Pass arrays or objects as JSON. | — |
| `huly spaces create <spaceType> <name>` | Create Generic Typed Space | `<spaceType>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--description <value>` — a string that will be trimmed<br>`--private` / `--no-private` — Whether the space is private.<br>`--auto-join` / `--no-auto-join` — Whether workspace members auto-join the space.<br>`--restricted` / `--no-restricted` — Whether transactions require an assigned space-type permission.<br>`--members <value>` — Initial members. SpaceType default members, owners, and role-assigned members are always included as well. Pass arrays or objects as JSON.<br>`--owners <value>` — Initial owners. Defaults to the calling Huly account; every owner is also made a member. Pass arrays or objects as JSON.<br>`--role-assignments <value>` — Optional role assignments created in the same MCP call after the space document is created. Pass arrays or objects as JSON.<br>`--description-file <path>` — read description as text |
| `huly spaces get <space>` | Get Space | `<space>` — a string that will be trimmed | — | `--include-archived` / `--no-include-archived` — Allow matching archived spaces by exact name. Defaults to false for name lookup. ID lookup can return archived spaces.<br>`--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces list` | List Spaces | — | — | `--include-archived` / `--no-include-archived` — Include archived spaces in results. Defaults to false, so only active/non-archived spaces are returned.<br>`--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum spaces to return (default: 50). |
| `huly spaces members add <space> <members>` | Add people to a space; members is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces members remove <space> <members>` | Remove people from a space; members is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<members>` — Members to add or remove. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces owners set <space> <owners>` | Replace space owners; owners is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<owners>` — Replacement owner list. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear owners. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--ensure-members` / `--no-ensure-members` — Also add each owner to members. Defaults to true. |
| `huly spaces permissions list` | List Space Permissions | — | — | `--scope <value>` — Filter permissions by Huly scope: space or workspace.<br>`--object-class <value>` — a string that will be trimmed<br>`--search <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum permissions to return (default: 50). |
| `huly spaces roles create <spaceType> <name> <permissions>` | Create Space Role Requires `--yes`. | `<spaceType>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed<br>`<permissions>` — Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | — |
| `huly spaces roles members add <space> <role> <members>` | Add space-role members; members is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<role>` — a string that will be trimmed<br>`<members>` — Members to add or remove from this role. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces roles members remove <space> <role> <members>` | Remove space-role members; members is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<role>` — a string that will be trimmed<br>`<members>` — Members to add or remove from this role. Each entry may be an account UUID, exact email address, or exact person name. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces roles members set <space> <role> <members>` | Replace space-role members; members is a JSON array Requires `--yes`. | `<space>` — a string that will be trimmed<br>`<role>` — a string that will be trimmed<br>`<members>` — Replacement member list for this role only. Each entry may be an account UUID, exact email address, or exact person name. Pass [] to clear this role. Pass arrays or objects as JSON. | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed |
| `huly spaces roles permissions set <spaceType> <role> <permissions>` | Set Space Role Permissions Requires `--yes`. | `<spaceType>` — a string that will be trimmed<br>`<role>` — a string that will be trimmed<br>`<permissions>` — Pass arrays or objects as JSON. | `--confirm` / `--no-confirm` — Must be true to acknowledge that this operation changes workspace access-control metadata. | — |
| `huly spaces types get <spaceType>` | Get Space Type | `<spaceType>` — a string that will be trimmed | — | — |
| `huly spaces types list` | List Space Types | — | — | `--target-class <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum space types to return (default: 50). |
| `huly spaces update <space>` | Update a generic space | `<space>` — a string that will be trimmed | — | `--class <value>` — a string that will be trimmed<br>`--type <value>` — a string that will be trimmed<br>`--name <value>` — a string that will be trimmed<br>`--description <value>` — New plain-text description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--private` / `--no-private` — Whether the space is private.<br>`--archived` / `--no-archived` — Whether the space is archived.<br>`--auto-join` / `--no-auto-join` — Whether workspace members should auto-join the space when supported. |
| `huly status-categories create <ofAttribute> <label> <defaultStatus>` | Create Generic Status Category | `<ofAttribute>` — a string that will be trimmed<br>`<label>`<br>`<defaultStatus>` — a string that will be trimmed | — | `--icon <value>`<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Pass arrays or objects as JSON.<br>`--order <value>` — Whole number. |
| `huly status-categories delete <category>` | Delete Generic Status Category Requires `--yes`. | `<category>` — a string that will be trimmed | — | `--of-attribute <value>` — a string that will be trimmed |
| `huly status-categories get <category>` | Get Generic Status Category | `<category>` — a string that will be trimmed | — | `--of-attribute <value>` — a string that will be trimmed |
| `huly status-categories list` | List Generic Status Categories | — | — | `--of-attribute <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum categories to return (default: 50). |
| `huly status-categories update <category>` | Update Generic Status Category | `<category>` — a string that will be trimmed | — | `--current-of-attribute <value>` — a string that will be trimmed<br>`--label <value>`<br>`--of-attribute <value>` — a string that will be trimmed<br>`--default-status <value>` — a string that will be trimmed<br>`--icon <value>`<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Pass arrays or objects as JSON.<br>`--order <value>` — Whole number. |
| `huly storage upload <filename> <contentType>` | Upload a file to Huly storage. filePath is read by the CLI process, fileUrl is fetched by the CLI process, data is canonical base64, and --data-base64-file encodes local bytes as base64. Provide exactly one source. | `<filename>` — a string that will be trimmed<br>`<contentType>` — a string that will be trimmed | — | `--file-path <value>` — Filesystem path read by the CLI process.<br>`--file-url <value>` — Remote URL fetched from the CLI process network.<br>`--data <value>` — Canonical RFC 4648 base64 file content; use --data-base64-file for local bytes.<br>`--data-base64-file <path>` — encode local bytes as canonical base64 |
| `huly support status get` | Get Support Status | — | — | — |
| `huly tags attached list` | List Attached Tags | — | `--object-id <value>` — a string that will be trimmed<br>`--object-class <value>` — a string that will be trimmed<br>`--space <value>` — a string that will be trimmed<br>`--collection <value>` — a string that will be trimmed | — |
| `huly tags categories create <label>` | Create Tag Category | `<label>` — a string that will be trimmed | — | `--target-class <value>` — a string that will be trimmed<br>`--default` / `--no-default` — Whether this is a default category (default: false) |
| `huly tags categories delete <category>` | Delete Tag Category Requires `--yes`. | `<category>` — a string that will be trimmed | — | — |
| `huly tags categories list` | List Tag Categories | — | — | `--target-class <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of categories to return (default: 50) |
| `huly tags categories update <category>` | Update Tag Category | `<category>` — a string that will be trimmed | — | `--label <value>` — a string that will be trimmed<br>`--default` / `--no-default` — New default flag |
| `huly tags create <targetClass> <title>` | Create Tag | `<targetClass>` — a string that will be trimmed<br>`<title>` — a string that will be trimmed | — | `--color <value>` — Huly platform color palette index from 0 through 23 (default: 0).<br>`--description <value>` — Tag description.<br>`--category <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly tags delete <targetClass> <tag>` | Delete Tag Requires `--yes`. | `<targetClass>` — a string that will be trimmed<br>`<tag>` — a string that will be trimmed | — | — |
| `huly tags list <targetClass>` | List Tags | `<targetClass>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed<br>`--title-search <value>` — Search tag titles by substring (case-insensitive where supported by Huly backend).<br>`--limit <value>` — Maximum number of tags to return (default: 50). |
| `huly tags update <targetClass> <tag>` | Update Tag | `<targetClass>` — a string that will be trimmed<br>`<tag>` — a string that will be trimmed | — | `--title <value>` — a string that will be trimmed<br>`--color <value>` — New Huly platform color palette index from 0 through 23.<br>`--description <value>` — New tag description. Pass null to clear; empty string is also accepted. Pass null to clear the field.<br>`--category <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly task-types create <name>` | Create Task Type | `<name>` — a string that will be trimmed | — | `--project-type <value>` — a string that will be trimmed<br>`--template-task-type <value>` — a string that will be trimmed |
| `huly task-types list` | List Task Types | — | — | `--project-type <value>` — a string that will be trimmed |
| `huly teamspaces create <name>` | Create Teamspace | `<name>` — a string that will be trimmed | — | `--description <value>` — Teamspace description<br>`--private` / `--no-private` — Whether the teamspace is private (default: false)<br>`--description-file <path>` — read description as text |
| `huly teamspaces delete <teamspace>` | Delete Teamspace Requires `--yes`. | `<teamspace>` — a string that will be trimmed | — | — |
| `huly teamspaces get <teamspace>` | Get a teamspace | `<teamspace>` — a string that will be trimmed | — | — |
| `huly teamspaces list` | List teamspaces | — | — | `--include-archived` / `--no-include-archived` — Include archived teamspaces in results (default: false, showing only active)<br>`--limit <value>` — Maximum number of teamspaces to return (default: 50) |
| `huly teamspaces update <teamspace>` | Update Teamspace | `<teamspace>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New description (null to clear) Pass null to clear the field.<br>`--archived` / `--no-archived` — Set archived status<br>`--description-file <path>` — read description as text |
| `huly templates categories list` | List Message Template Categories | — | — | `--limit <value>` — Maximum number of template categories to return (default: 50). |
| `huly templates fields list` | List Message Template Fields | — | — | `--category <value>` — Optional template field category ID or exact raw label string.<br>`--search <value>` — Optional case-insensitive substring search over raw template field labels.<br>`--limit <value>` — Maximum number of template fields to return (default: 50). |
| `huly templates get <template>` | Get Message Template | `<template>` — Template ID or exact template title. If title is ambiguous, also provide category. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate template title lookup. |
| `huly templates list` | List Message Templates | — | — | `--category <value>` — Optional category filter. Accepts a category ID or exact category name.<br>`--search <value>` — Optional case-insensitive substring search over template titles.<br>`--limit <value>` — Maximum number of templates to return (default: 50). |
| `huly templates render <template>` | Render a message template; pass --values as a JSON object | `<template>` — Template ID or exact template title. If title is ambiguous, also provide category. | — | `--category <value>` — Optional category ID or exact category name used to disambiguate template title lookup.<br>`--values <value>` — Optional caller-provided placeholder values. Each field is a template field ID from placeholderFieldIds; duplicate field entries use the last value. Pass arrays or objects as JSON. |
| `huly tests case create <project> <suite> <name>` | Create Test Case | `<project>` — a string that will be trimmed<br>`<suite>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--description <value>` — Test case description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--type <value>` — Test case type (default: functional)<br>`--priority <value>` — Test case priority (default: medium)<br>`--status <value>` — Test case status (default: draft)<br>`--assignee <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly tests case delete <project> <testCase>` | Delete Test Case Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<testCase>` — a string that will be trimmed | — | — |
| `huly tests case get <project> <testCase>` | Get Test Case | `<project>` — a string that will be trimmed<br>`<testCase>` — a string that will be trimmed | — | — |
| `huly tests case update <project> <testCase>` | Update Test Case | `<project>` — a string that will be trimmed<br>`<testCase>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New test case description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--type <value>` — New test case type<br>`--priority <value>` — New priority<br>`--status <value>` — New status<br>`--assignee <value>` — New assignee name or email (null to unassign) Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests cases list <project>` | List Test Cases | `<project>` — a string that will be trimmed | — | `--suite <value>` — a string that will be trimmed<br>`--assignee <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of test cases to return (default: 50) |
| `huly tests plan create <project> <name>` | Create Test Plan | `<project>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--description <value>` — Description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--description-file <path>` — read description as text |
| `huly tests plan delete <project> <plan>` | Delete Test Plan Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed | — | — |
| `huly tests plan get <project> <plan>` | Get Test Plan | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed | — | — |
| `huly tests plan item add <project> <plan> <testCase>` | Add Test Plan Item | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed<br>`<testCase>` — a string that will be trimmed | — | `--assignee <value>` — a string that will be trimmed |
| `huly tests plan item remove <project> <plan> <item>` | Remove Test Plan Item Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed<br>`<item>` — a string that will be trimmed | — | — |
| `huly tests plan run <project> <plan>` | Run Test Plan | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed | — | `--run-name <value>` — a string that will be trimmed<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds |
| `huly tests plan update <project> <plan>` | Update Test Plan | `<project>` — a string that will be trimmed<br>`<plan>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests plans list <project>` | List Test Plans | `<project>` — a string that will be trimmed | — | `--limit <value>` — Max items to return (default: 50) |
| `huly tests projects list` | List Test Projects | — | — | `--limit <value>` — Maximum number of projects to return (default: 50) |
| `huly tests result create <project> <run> <testCase>` | Create Test Result | `<project>` — a string that will be trimmed<br>`<run>` — a string that will be trimmed<br>`<testCase>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--status <value>` — Status: untested, blocked, passed, failed<br>`--assignee <value>` — a string that will be trimmed |
| `huly tests result delete <project> <result>` | Delete Test Result Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<result>` — a string that will be trimmed | — | — |
| `huly tests result get <project> <result>` | Get Test Result | `<project>` — a string that will be trimmed<br>`<result>` — a string that will be trimmed | — | — |
| `huly tests result update <project> <result>` | Update Test Result | `<project>` — a string that will be trimmed<br>`<result>` — a string that will be trimmed | — | `--status <value>` — Status: untested, blocked, passed, failed<br>`--assignee <value>` — Assignee email or name, or null to unassign Pass null to clear the field.<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests results list <project> <run>` | List Test Results | `<project>` — a string that will be trimmed<br>`<run>` — a string that will be trimmed | — | `--limit <value>` — Max items to return (default: 50) |
| `huly tests run create <project> <name>` | Create Test Run | `<project>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--description <value>` — Description in markdown. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links.<br>`--due-date <value>` — Due date as Unix timestamp in milliseconds<br>`--description-file <path>` — read description as text |
| `huly tests run delete <project> <run>` | Delete Test Run Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<run>` — a string that will be trimmed | — | — |
| `huly tests run get <project> <run>` | Get Test Run | `<project>` — a string that will be trimmed<br>`<run>` — a string that will be trimmed | — | — |
| `huly tests run update <project> <run>` | Update Test Run | `<project>` — a string that will be trimmed<br>`<run>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — Description in markdown, or null to clear. Markdown links to current-workspace Huly browse URLs with _class, _id, and label become native Huly references; external URLs and other-workspace browse URLs stay normal links. Pass null to clear the field.<br>`--due-date <value>` — Due date (ms timestamp), or null to clear Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests runs list <project>` | List Test Runs | `<project>` — a string that will be trimmed | — | `--limit <value>` — Max items to return (default: 50) |
| `huly tests suite create <project> <name>` | Create Test Suite | `<project>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--description <value>` — Suite description<br>`--parent <value>` — a string that will be trimmed<br>`--description-file <path>` — read description as text |
| `huly tests suite delete <project> <suite>` | Delete Test Suite Requires `--yes`. | `<project>` — a string that will be trimmed<br>`<suite>` — a string that will be trimmed | — | — |
| `huly tests suite get <project> <suite>` | Get Test Suite | `<project>` — a string that will be trimmed<br>`<suite>` — a string that will be trimmed | — | — |
| `huly tests suite update <project> <suite>` | Update Test Suite | `<project>` — a string that will be trimmed<br>`<suite>` — a string that will be trimmed | — | `--name <value>` — a string that will be trimmed<br>`--description <value>` — New suite description (null to clear) Pass null to clear the field.<br>`--description-file <path>` — read description as text |
| `huly tests suites list <project>` | List Test Suites | `<project>` — a string that will be trimmed | — | `--parent <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of suites to return (default: 50) |
| `huly time log <project> <identifier> <value>` | Log time in hours against an issue | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed<br>`<value>` — Time spent in hours (Huly native unit). Examples: 0.25 = 15 minutes; 8 = one work day. | — | `--description <value>` — Description of work done |
| `huly time reports detailed get <project>` | Get Detailed Time Report | `<project>` — a string that will be trimmed | — | `--from <value>` — Filter entries from this timestamp<br>`--to <value>` — Filter entries until this timestamp |
| `huly time reports get <project> <identifier>` | Get Time Report | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | — |
| `huly time reports list` | List Time Spend Reports | — | — | `--project <value>` — a string that will be trimmed<br>`--from <value>` — Filter entries from this timestamp<br>`--to <value>` — Filter entries until this timestamp<br>`--limit <value>` — Maximum number of entries to return (default: 50) |
| `huly time timers start <project> <identifier>` | Start an issue timer | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | — |
| `huly time timers stop <project> <identifier>` | Stop an issue timer | `<project>` — a string that will be trimmed<br>`<identifier>` — a string that will be trimmed | — | — |
| `huly time work-slots list` | List Work Slots | — | — | `--employee-id <value>` — a string that will be trimmed<br>`--from <value>` — Filter slots from this timestamp<br>`--to <value>` — Filter slots until this timestamp<br>`--limit <value>` — Maximum number of slots to return (default: 50) |
| `huly user-statuses list` | List User Statuses | — | — | `--online` / `--no-online` — Optional presence filter. Use true for currently connected users, false for offline records.<br>`--user <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of user status records to return (default: 50, maximum: 200). |
| `huly views filtered get <filteredView>` | Get Filtered View | `<filteredView>` — FilteredView _id or exact saved view name. | — | `--attached-to <value>` — Optional raw Huly app/resource string such as board:app:Board to disambiguate exact-name matches. |
| `huly views filtered list` | List Filtered Views | — | — | `--attached-to <value>` — a string that will be trimmed<br>`--visibility <value>` — Filter saved filtered views by whether the current account is in the saved view users list.<br>`--name-search <value>` — Optional saved view name substring search.<br>`--limit <value>` — Maximum number of saved filtered views to return (default: 50). |
| `huly views viewlets list` | List Viewlets | — | — | `--attach-to <value>` — a string that will be trimmed<br>`--viewlet <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum number of viewlets to return (default: 50). |
| `huly workbench applications list` | List Workbench Applications | — | — | `--alias <value>` — a string that will be trimmed<br>`--alias-search <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum applications to return (default: 50). |
| `huly workflow-statuses create <ofAttribute> <name>` | Create Generic Workflow Status | `<ofAttribute>` — a string that will be trimmed<br>`<name>` — a string that will be trimmed | — | `--category <value>` — a string that will be trimmed<br>`--color <value>` — Huly numeric color token or non-empty numeric gradient token array. Pass arrays or objects as JSON.<br>`--description <value>` — a string that will be trimmed |
| `huly workflow-statuses delete <status>` | Delete Generic Workflow Status Requires `--yes`. | `<status>` — a string that will be trimmed | — | `--of-attribute <value>` — a string that will be trimmed |
| `huly workflow-statuses get <status>` | Get Generic Workflow Status | `<status>` — a string that will be trimmed | — | `--of-attribute <value>` — a string that will be trimmed |
| `huly workflow-statuses list` | List Generic Workflow Statuses | — | — | `--of-attribute <value>` — a string that will be trimmed<br>`--category <value>` — a string that will be trimmed<br>`--limit <value>` — Maximum statuses to return (default: 50). |
| `huly workflow-statuses update <status>` | Update Generic Workflow Status | `<status>` — a string that will be trimmed | — | `--current-of-attribute <value>` — a string that will be trimmed<br>`--name <value>` — a string that will be trimmed<br>`--of-attribute <value>` — a string that will be trimmed<br>`--category <value>` — New category ID or exact label; pass null to remove the relationship. Categories may be shared across status attributes. Pass null to clear the field.<br>`--color <value>` — New color; pass null to clear it. Pass arrays or objects as JSON. Pass null to clear the field.<br>`--description <value>` — New description; pass null to clear it. Pass null to clear the field. |
| `huly workspace access-links create` | Create a workspace access link Requires `--yes`. | — | — | `--role <value>` — Workspace role granted by the link. Defaults to GUEST.<br>`--first-name <value>` — a string that will be trimmed<br>`--last-name <value>` — a string that will be trimmed<br>`--navigate-url <value>` — Optional URL/path Huly should open after the link is used.<br>`--spaces <value>` — Optional Huly space IDs this link should grant access to. Use list_teamspaces, list_card_spaces, or other list tools to discover space IDs. Pass arrays or objects as JSON.<br>`--not-before <value>` — Unix timestamp in seconds before which a non-personalized link is invalid.<br>`--expiration <value>` — Unix timestamp in seconds after which the link expires.<br>`--personalized` / `--no-personalized` — Set false for anonymous reusable guest links. Anonymous links require notBefore and expiration. |
| `huly workspace create <name>` | Create a workspace Requires `--yes`. | `<name>` — a string that will be trimmed | — | `--region <value>` — Region for the workspace (optional) |
| `huly workspace delete` | Delete the configured workspace Requires `--yes`. | — | — | — |
| `huly workspace guest-settings update` | Update workspace guest access settings Requires `--yes`. | — | — | `--allow-read-only` / `--no-allow-read-only` — Allow read-only guests<br>`--allow-sign-up` / `--no-allow-sign-up` — Allow guest sign-up |
| `huly workspace info get` | Get Workspace Info | — | — | — |
| `huly workspace list` | List Workspaces | — | — | `--limit <value>` — Maximum number of workspaces to return (default: 50) |
| `huly workspace members list` | List Workspace Members | — | — | `--limit <value>` — Maximum number of members to return (default: 50) |
| `huly workspace members role update <accountId> <role>` | Update a workspace member role Requires `--yes`. | `<accountId>` — a string that will be trimmed<br>`<role>` — New role for the member | — | — |
| `huly workspace profile get` | Get User Profile | — | — | — |
| `huly workspace profile update` | Update the current user's profile; pass null to clear nullable fields Requires `--yes`. | — | — | `--bio <value>` — Bio text (null to clear) Pass null to clear the field.<br>`--city <value>` — City (null to clear) Pass null to clear the field.<br>`--country <value>` — Country (null to clear) Pass null to clear the field.<br>`--website <value>` — Website URL (null to clear) Pass null to clear the field.<br>`--social-links <value>` — Social links as key-value pairs (null to clear) Pass arrays or objects as JSON. Pass null to clear the field.<br>`--is-public` / `--no-is-public` — Whether profile is public |
| `huly workspace regions get` | Get Regions | — | — | — |
<!-- CLI_COMMAND_REFERENCE_END -->
